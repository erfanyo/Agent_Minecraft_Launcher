# 测试指南(2026-08 批次:设置标签页 / 镜像策略 / AI 视觉联动 / Agent 功能)

> 适用本轮改动:设置对话框拆标签页、镜像源下载策略、AI 图片按钮联动、帮助菜单移除、
> 以及 agent(内置本地模型 + 工具调用)相关功能。
> 本指南按"一步一步慢慢测"编写:每步做完在 `[ ]` 里打勾,异常记录到文末问题表。
> 预计耗时:约 30–60 分钟(含可选的重启验证)。

---

## 0. 测试准备(必做)

- [ ] **备份配置**:把 `AMCL/config.json` 复制一份存到别处(如桌面 `config.json.bak`)。
      测试中会反复改设置,出问题可以一键还原。
- [ ] **确认运行方式**:源码运行 → 在项目目录执行 `python main.py`(建议用 `.venv`);
      或直接运行打包好的 exe。
- [ ] **记录基线**:打开 `AMCL/config.json`,记录当前 `mirror_strategy`、`mirror_source`、
      `ai_multimodal`、`ai_provider` 的值(方便对照迁移/联动是否生效)。
- [ ] **准备一个已下载好的游戏实例**(如 1.21 原版),第 5 部分"启动实例"要用。

> ⚠️ 修改配置时的通用预期:所有设置点"确定"后写入 `AMCL/config.json`;
> 除了界面语言要重启生效,其余设置立即生效。

---

## 1. 设置对话框:标签页

入口:菜单栏 **设置 → 设置对话框…**

- [ ] **1.1** 打开设置对话框,应看到 4 个标签页:**游戏 / 界面 / AI 助手 / 镜像源**。
      (旧版是一个长表单,现在必须是标签页形式)
- [ ] **1.2 游戏页**:改游戏名为 `Test2026`、内存拉到 4GB、取消版本隔离 → 确定。
      重新打开设置,值应保持;`AMCL/config.json` 里 `username/memory_gb/version_isolation` 已更新。
- [ ] **1.3 界面页**:界面语言切到 English → 确定 → **重启启动器**,界面应变成英文;
      再切回"自动(跟随系统)"。
      界面模式切"专家"→ 首页提示/科普应变少;切回"新手"。
- [ ] **1.4 AI 助手页**:应包含:AI 服务商(下拉 9 项,含"内置本地模型")、接口地址、
      API 密钥、模型、文件权限、上下文窗口、图片输入、游戏内 AI。
      随意改一项 → 确定 → 重开,值保持。
- [ ] **1.5 镜像源页**:应有两个下拉:下载策略(4 项)+ 镜像站(默认 BMCLAPI),下方是自定义镜像管理区。

---

## 2. 镜像源与下载策略

入口:设置 → 镜像源页,或菜单 **设置 → 镜像源…**(应直接跳到镜像源标签页)。

### 2.1 默认策略:官方优先(官方慢或失败时换镜像)

- [ ] **2.1.1** 策略选"官方优先(官方慢或失败时换镜像)",镜像站 BMCLAPI → 确定。
- [ ] **2.1.2** 下载一个新版本(或刷新版本列表):日志/进度应表现为——先连官方,
      连不上或很慢(约 6 秒)后自动切到 bmclapi 继续下载,最终安装成功。
      (断网或改 hosts 模拟官方不可达,可更明显看到"自动换镜像")

### 2.2 镜像优先

- [ ] **2.2.1** 策略选"镜像优先(镜像失败才回官方)"→ 确定。
- [ ] **2.2.2** 再下载一个文件:应直接从 bmclapi 开始(不走官方)。

### 2.3 只用官方源

- [ ] **2.3.1** 策略选"只用官方源"→ 镜像站下拉应**变灰禁用**。
- [ ] **2.3.2** 下载时只连官方,即使失败也不走镜像(可断网验证:下载最终报错,而不是换源)。

### 2.4 只用镜像源

- [ ] **2.4.1** 策略选"只用镜像源"→ 下载只走镜像站,失败也不回官方。

### 2.5 自定义镜像管理

- [ ] **2.5.1** 添加:名称填 `我的镜像`,地址填 `https://mirror.example.com` → 点"添加"。
      下拉"镜像站"应多出"自定义: 我的镜像"且自动选中;说明文字显示该地址。
- [ ] **2.5.2** 选中它 → 确定 → 下载应尝试该地址(失败后按策略回退)。
- [ ] **2.5.3** 名称/地址留空点添加 → 应弹提示"请填写名称和地址",不崩溃。
- [ ] **2.5.4** 地址不以 http(s) 开头 → 应弹提示,拒绝添加。
- [ ] **2.5.5** 删除:在自定义列表选中"我的镜像"→ 删除选中 → 列表清空,
      镜像站自动退回 BMCLAPI。若删除的是正在使用的镜像,应自动退回 BMCLAPI。
- [ ] **2.5.6** 添加两个自定义镜像,全部删除后再添加一个,id 不应重复(不影响使用,观察即可)。

### 2.6 旧配置迁移(可选,验证兼容)

- [ ] **2.6.1** 关闭启动器,手动把 `AMCL/config.json` 的 `mirror_source` 改成 `"official"`、
      删掉 `mirror_strategy` 字段 → 重新启动。
- [ ] **2.6.2** 打开镜像源页:策略应显示"官方优先",镜像站显示 BMCLAPI(旧"官方源"已迁移)。
- [ ] **2.6.3** 再手动改回 `mirror_source: "bmclapi"` 且无 `mirror_strategy` → 重启后
      策略应显示"镜像优先"(保留旧行为)。

---

## 3. 菜单调整:帮助移除 / 检查更新

- [ ] **3.1** 菜单栏**不应再有"帮助"菜单**(旧版只有"检查更新"那一项)。
- [ ] **3.2** 菜单 **设置 → 检查更新…** 应仍可用:弹出更新对话框,能检查 AMCL 启动器 + bridge-mod 版本。
- [ ] **3.3** 菜单 **设置 → 镜像源…** 应直接打开设置对话框并定位到镜像源页。

---

## 4. AI 图片按钮联动(多模态)

入口:设置 → AI 助手页;对照:右侧 AI 对话栏输入框右下角的 📷 / 🖼 按钮。

- [ ] **4.1** AI 助手页勾选"允许给 AI 发图片(需要你选的模型本身支持看图)"→ 确定。
      AI 输入框应出现 📷(添加图片)和 🖼(最近截图)按钮;占位提示含"Ctrl+V 可粘贴图片"。
- [ ] **4.2** 取消勾选 → 确定 → 两个按钮应**隐藏**,占位提示不再提粘贴图片。
- [ ] **4.3 自动判定(服务商联动)**:AI 服务商切到"内置本地模型"→ 图片输入应**自动取消勾选**
      (本地模型不支持看图);切到"OpenRouter"→ 应**自动勾选**(默认支持看图)。
      模型名含 vision/vl 等关键词(如 `gpt-4o`)→ 自动勾选。
- [ ] **4.4 手动覆盖**:自动判定后仍可手动勾选/取消(勾选框始终可点)。
- [ ] **4.5 关闭状态下粘贴图片**(Ctrl+V 截图):应提示"当前模型不支持图片输入",图片不进入待发列表。
- [ ] **4.6 关闭状态下点隐藏按钮不可行(按钮已隐藏),确认对话栏布局正常、发送按钮位置不重叠**。

---

## 5. Agent 相关功能(重点 · 规划落地中)

> 说明:内置本地模型 + 工具调用(grammar 约束)已在代码里接通,但属"落地中",
> 部分环节(如模型懒加载的下载进度、失败自动落云端)以"能观察到正确行为"为准。

### 5.1 内置本地模型接入

- [ ] **5.1.1** 设置 → AI 助手页,服务商选"内置本地模型(免费 · 无密钥,需下载约500MB)"。
      接口地址应清空,模型名自动填 `qwen3.5-0.8b-xlam-q4km` → 确定。
- [ ] **5.1.2 首次使用触发下载**:打开 AI 对话栏,随便问一句(如"你好")。
      若模型未下载,应弹出"正在下载本地模型(约500MB,镜像优先)…"进度框;
      文件最终落在 **`AMCL/models/`**(qwen3.5-0.8b-function-calling-xlam.q4_k_m.gguf,
      约 505MB),校验通过才算下载完成。
- [ ] **5.1.3 模型已下载后**:再次提问应直接走本地模型,不再弹下载框(懒加载生效)。
- [ ] **5.1.4 关闭自动下载**(暂无界面,需手动改 `AMCL/config.json` 的 `ai_local_auto_download` 为
      `false` 后重启):首次使用应提示走云端,而不是弹下载框。测完记得改回 `true`。

### 5.2 Agent 启动实例(核心用例)

- [ ] **5.2.1** 确认至少有一个已下载好的实例;选中它。
- [ ] **5.2.2** 在 AI 对话栏输入:"**启动这个实例**" → 应调用 `launch_game` 工具,
      游戏窗口被拉起;对话区显示"✅ 已执行「launch_game」"及结果。
      (云端 DeepSeek 路径与本地 local_builtin 路径都测一遍)
- [ ] **5.2.3** 输入"**启动 xxx 实例**"(xxx = 实例 id 或名字)→ 应启动**已有**实例,
      **不应**误解为"创建新实例"(§8.1 修复点:launch_game vs install_instance 必须区分)。
- [ ] **5.2.4** 输入"**创建一个 1.21 的 Fabric 实例**" → 应调用 `install_instance` 创建新实例,
      而不是启动旧实例。
- [ ] **5.2.5** 输入模糊指令(如"帮我弄个能玩的")→ 应触发 `ask_user` 确认弹窗,
      而不是擅自选工具(§8.1 修复点:歧义请求必须问用户)。

### 5.3 失败链路与权限

- [ ] **5.3.1** 本地模型路径下故意让它做不了的事(如断网 + 纯本地):应显示"本地推理失败…"
      并**自动落云端**继续(失败链路:本地 → 规则引擎 → 云端)。
- [ ] **5.3.2 权限联动**:文件权限=只读时,让 AI"装个 Mod / 改设置" → 应拒绝并提示
      "当前是只读权限…";权限切"工作区可写"后同样指令应能执行。
- [ ] **5.3.3 上下文注入**:提问前选中一个实例,AI 应能说出当前实例/设置(启动器上下文注入生效)。

### 5.4 游戏内 AI 通道(设置项,联动规划中)

- [ ] **5.4.1** 设置 → AI 助手页 → 游戏内 AI:三档(关闭/云端/本地)都能选择并保存到 config。
- [ ] **5.4.2** (联动逻辑规划中,此项仅记录)游戏启动时模型卸载/保留策略,以观察为准,不强制。

---

## 6. 文件放置检查(不污染用户目录)

> 原则:启动器需要的一切文件都放在启动器自己的目录里,绝不在用户系统目录"到处拉屎"。

- [ ] **6.1** 跑完上面全部流程后,检查以下目录**不应出现启动器/模型/AI 生成的文件**:
      `%APPDATA%`(除 `AgentMinecraftLauncher` 兜底目录外)、`%TEMP%`、`文档`、`桌面`、`C:\Users\<你>\` 根目录。
- [ ] **6.2** `AMCL/` 目录结构应为:
      ```
      AMCL/
      ├─ config.json            ← 启动器设置(原始用途)
      ├─ models/                ← 本地模型 + manifest.json(model_registry)
      └─ runtime/llama-cpp/     ← llama.cpp 本地推理运行时(local_ai)
      ```
- [ ] **6.3** 新下载的模型文件确实在 `AMCL/models/`,不在别处;llama-server 等程序确实在 `AMCL/runtime/llama-cpp/`。
- [ ] **6.4** 游戏数据仍在启动器目录内的 `.minecraft/`(可配置,设置里改路径后仍应在指定位置)。

---

## 附录 A:问题记录表

| # | 步骤号 | 现象(实际) | 期望 | 是否可复现 | 备注 |
|---|--------|-----------|------|-----------|------|
| 1 |        |           |      |           |      |
| 2 |        |           |      |           |      |
| 3 |        |           |      |           |      |

## 附录 B:回归抽查(改动后顺带确认没坏)

- [ ] 启动游戏 → 退出,正常。
- [ ] 刷新版本列表 / 下载新版本,正常。
- [ ] 菜单 文件(刷新/导入整合包/打开游戏目录/退出)均可用。
- [ ] AI 对话栏发送/工具调用/技能管理入口正常。
