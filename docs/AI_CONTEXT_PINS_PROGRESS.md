# AI 上下文图钉 · 2026-09-07 续接记录

## 2026-09-08 提交时状态（优先于下方历史记录）

用户已授权本地提交，未要求推送或打包。图钉已支持实例列表、下载目标实例、下载资源、下载实例四类菜单与游戏版本树、普通文字/按钮/标签页及插件描述。
速度脱离阈值为 900 逻辑像素/秒；180ms 停稳吸附、快速脱离后 240ms 冷却。高亮框改为目标窗口内子控件，避免浮层干扰选项卡命中。
固定目标保留可左键取消的角标。18 项组件测试通过；仍需真实窗口和模型端到端验证。自动跟随选择、网页接入尚未实现。
下方“尚未提交”、测试数量及阈值等为开发过程历史，不代表当前状态。

## 需求

将 demos/ai-context.html 的拖图钉交互接入 Qt 启动器。用户希望尽量覆盖界面，未来可扩展到网页。额度中断后从这里继续，不要重做已完成部分。

## 已接入（源码，尚未打包或提交）

- ai_context_pins.py：独立组件；拖动/点击放置、Esc 取消、高亮目标、最多 8 个快照、去重、脱敏、移除、查看内容、超过 3 个折叠。
- assistant.py：AI Dock 输入区上方入口；发送时将固定内容放入用户消息；本地路由跳过规则捷径；现有权限/确认流程不变。直接 / 指令不附加图钉。
- instance_manager.py：实例概览和 Mod 列表按鼠标所在行捕获；记录实例、目录、版本、加载器。点击标签回到对应实例及 Mod；失效目标不修改文件。
- main.py：游戏日志选段，未选中文本时取末尾 4000 字符。
- 已固定内容是快照，不随页面变化自动更新，不自动上传，不跨会话持久化；实际发送时才交给所选模型。
- 显式 ai_pin_provider(global_position) 扩展接口，不随便抓取任意控件，避免账号/密码等敏感设置混入上下文。

## 验证与环境

- test_ai_context_pins.py：3 项组件测试（快照/脱敏/移除、去重/上限、取消/空目标）已通过。
- assistant.py、main.py、instance_manager.py、ai_context_pins.py 编译检查通过。
- 隔离环境无法启动 .venv/Scripts/python.exe，但获批在隔离环境外运行成功（Qt 6.11.2），不是需要重建虚拟环境。
- 尚需真实窗口鼠标拖动、跨页面定位、不同主题与真实模型端到端测试。

## 接下来

### 后续增量已完成

- 最新交互：AI Dock 普通按钮、QTabBar 标题（包括实例数量）支持固定；图钉管理区自身与敏感区域仍排除。
- 拖动悬停 180ms 后吸附，6px 内轻微抖动不重计时；已吸附时移动达到 28px 才脱离。遵循全局动画开关。
- 固定目标上保留可点击角标；左键移除快照和角标。列表用持久模型索引跟随滚动，目标销毁后角标随之销毁，不把标记转移给其他行。
- 当前共 14 项组件测试通过；真实窗口手感仍需用户试用。

- 已安装资源改为本地图标紧凑卡片（installed_resource_cards.py），异步元数据读取、缓存、保守不兼容提示。
- 列表拖动只高亮鼠标下的单项可见区域，空白和提示行不可固定。
- 下载新资源搜索结果支持 Modrinth/CurseForge 项目快照，包含来源、项目 ID、简介和目标实例；不冒充已选具体版本。
- 普通 QLabel / 按钮文字可固定，输入框不自动抓取；显式 provider 优先，敏感区域优先排除。
- PluginAPI 新增 bind_ai_context 和 exclude_ai_context，hello 示例与公开 API 文档已更新。
- 资源、普通文字、插件标签点击显示快照预览；尚未实现这些类别的跨页面定位。
- 最新共 10 项组件测试通过，变更模块语法检查通过。以下旧待办中资源搜索接入已完成；原处角标、自动跟随和真实窗口全流程验证仍待完成。

1. 补真实窗口交互验证，确认鼠标捕获、取消、浮动 Dock，以及日志页定位。
2. 为原对象增加常驻图钉角标；不能仅依靠 Dock 标签。完善定位后的轻量高亮。
3. 扩大明确支持的目标：资源搜索卡片、实例列表、设置项（敏感项禁止）。沿用 provider 接口，不抓取整页隐藏文本。
4. 评估 Demo 中“跟随选择”与手动固定的组合；目前只有用户明确固定的快照，尚无自动跟随。
5. 网页目前未实现；后续通过受控浏览器扩展/内嵌网页适配，网页内容必须视为不可信数据，不授予执行权限。

不要误报为完整 Demo 全量落地，也不要擅自提交或发布。
