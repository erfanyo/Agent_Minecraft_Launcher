# -*- coding: utf-8 -*-
"""
「已临时弃用 / 废案但未移除」的前端功能登记表(纯数据)。

**用途**:某些前端功能效果不够好 / 方向变了,先**隐藏主入口、临时弃用**,但代码还没删。
为避免用户或自己"找不到/忘了",在**设置对话框 → 界面**里登记这些功能:
显示 名称 + 当前状态(临时弃用 / 废案未移除)+ 说明 + 可选"临时查看"入口。

**规划**:以后删某个前端功能前,先登记到这;真删除了就移出本表。
条目字段: id / name / status(临时弃用|废案未移除)/ note / reopen(可选,打开临时入口的 key)。
"""

DEPRECATED_FEATURES = [
    {
        "id": "instance_icons_view",
        "name": "实例:大图标显示(视图)",
        "status": "临时弃用",
        "note": "之前「查看 → 实例:大图标」的大图标列表视图,已连同「查看」菜单一起移除、临时弃用。"
                "实例列表统一为列表视图;若想要大图标封面可复用「我的版本」右键的封面图。代码仍保留(set_view_mode)。",
        "reopen": None,
    },
]


def get_deprecated() -> list:
    return list(DEPRECATED_FEATURES)
