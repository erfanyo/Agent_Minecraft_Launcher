# Agent Minecraft Launcher — 项目规划(草稿 v1)

> 这是一份"活文档",随着讨论不断更新。当前状态:方向已定 ✅、技术栈已定 ✅、AI 方案已定 ✅,下一步进入**阶段 0:环境准备**。

## 0. 已确认的决策(记录在案)

- 产品类型:AI 助手型启动器,给朋友/社区用
- 使用场景:**原版 + 模组 + 服务器全都要**
- 技术栈:**Python + PySide6**
- AI 接入:**可配置**,默认 DeepSeek API,可选 Ollama 本地模型(两者都是 OpenAI 兼容接口)
- 账号:先离线登录,微软正版登录暂缓

## 1. 产品定位

- **核心价值**:别人家的启动器只会"启动游戏",我们的启动器能**回答问题、帮你诊断报错、自动装 Mod**
- **成功标准(给朋友用的底线)**:打开就能用、崩溃有解释、下载不卡死

## 2. 功能拆解

### 2.1 启动器基础功能(必修课,没有这些就不是启动器)

| 功能 | 说明 | 难度 |
|---|---|---|
| 版本管理 | 从 Mojang 官方 API 拉取版本列表,选择要玩的版本 | 中 |
| 游戏文件下载 | 下载游戏本体 jar + libraries(依赖库)+ assets(资源文件) | 中(网络镜像关键) |
| Java 管理 | 检测系统里的 Java,没有就自动下载一个 | 低 |
| 启动游戏 | 拼装启动命令,拉起 java 进程,窗口显示日志 | 中(核心中的核心) |
| 日志与崩溃检测 | 捕捉游戏输出,检测崩溃,保存崩溃报告 | 中 |
| 账号登录 | 先做**离线登录**(简单),微软正版登录暂缓 | 低 → 高 |
| 设置界面 | 内存分配、游戏目录、Java 路径、分辨率 | 低 |
| 服务器快捷入口 | 保存常用服务器地址,一键复制/跳转(朋友玩服务器的刚需) | 低 |

### 2.2 AI 助手功能(亮点,按"性价比"排序)

1. **崩溃日志分析**(最实用):游戏崩了 → 启动器自动把 `logs/latest.log` 和崩溃报告发给 AI → AI 告诉你哪里崩了、怎么修。这是第一个能让朋友"哇"的功能。
2. **对话问答**:在启动器里直接问"怎么给服务器装插件""这个报错什么意思",AI 结合你的游戏环境回答。
3. **自然语言操作**:"帮我把内存调到 4G" → 启动器自动改设置。
4. **Mod 搜索与一键安装**:通过 Modrinth API 搜 Mod、下载、装进游戏(需要先支持 Fabric/Forge 加载器)。
5. **进阶玩法**(以后再说):"帮我装一个空岛整合包"这种多步任务。

### 2.3 不做的事(明确边界,防止跑偏)

- 不做服务器托管、不做反作弊、不做账号交易
- 不碰正版验证绕过(违法),离线登录仅用于学习/内网

## 3. 技术栈建议(待讨论)

### 3.1 界面+逻辑框架 —— ✅ 已确定:Python + PySide6

| 方案 | 优点 | 缺点 | 评价 |
|---|---|---|---|
| **Python + PySide6** | AI 生态最强;语法对新手友好;启动器逻辑用 Python 绰绰有余 | 打包体积较大;界面不如 Web 精致 | **推荐** |
| Electron + TypeScript | 界面最漂亮、上限高 | 内存大户(150MB+);AI 生态弱;对新手上手偏难 | 界面优先可选 |
| Tauri (Rust) | 轻量、漂亮 | Rust 学习曲线陡 | 不推荐新手 |
| 改开源 Java 启动器(HMCL) | 功能现成 | 代码庞大、难懂难改;AI 集成很别扭 | 不推荐 |

**初步推荐:Python + PySide6。** 理由:你的核心卖点是 AI,Python 是 AI 世界的母语;而"启动 Minecraft"本质上只是"下载文件 + 拼一条命令行",Python 完全能胜任。

> ✅ **2025 已拍板**:Python + PySide6。界面朴素没关系,功能靠谱、AI 好用优先;逻辑与界面分离,未来可换 Web 壳。

### 3.2 AI 怎么接(三种,可做成可配置)

| 方案 | 费用 | 优点 | 缺点 |
|---|---|---|---|
| **DeepSeek API**(推荐默认) | 极低(几块钱能用很久) | 国内直连、便宜、效果好 | 需要联网 |
| OpenAI / 其他云端 API | 中等 | 效果最强 | 国内访问麻烦 |
| Ollama 本地模型(如 Qwen) | 免费、离线 | 隐私好、无费用 | 吃内存/显卡,慢 |

**已确定:做成可配置,默认 DeepSeek,高级设置里可换 Ollama 本地。**(两者都是 OpenAI 兼容接口,切换成本低)

## 4. 分阶段路线图(MVP 思维:先跑通,再变好)

```
阶段 0  环境准备
        装 Python、装编辑器,学"够用"的 Python 基础(我会带你把范围划得很小)
        ✅ 产出:能跑 hello world + 一个能弹出的窗口

阶段 1  启动器核心(最难啃的 30%,先做这个)
        版本列表 → 下载游戏 → 启动成功 → 显示日志
        ✅ 产出:能离线启动 Minecraft 的启动器雏形

阶段 2  AI 助手 v1
        对话窗口 + 崩溃日志分析
        ✅ 产出:游戏崩了,AI 告诉你为什么

阶段 3  Mod 管理
        Fabric 加载器安装 + Modrinth 搜索安装
        ✅ 产出:搜 Mod、点一下装上

阶段 4  微软正版登录(暂缓项,想清楚再做)

阶段 5  打包发布
        打包成 exe 给朋友、处理杀软误报、界面打磨
```

## 5. 关键外部资源(全部免费公开)

| 资源 | 用途 |
|---|---|
| `launchermeta.mojang.com` 版本清单 API | 获取所有 Minecraft 版本及下载地址 |
| BMCLAPI 等国内镜像 | 中国网络下的下载加速(几乎是必须) |
| `meta.fabricmc.net` API | 查 Fabric 加载器版本、生成安装信息 |
| Modrinth API / CurseForge API | 搜 Mod、下载 Mod |
| 游戏内 `logs/latest.log`、`crash-reports/` | 崩溃分析的输入 |

## 6. 风险与注意

1. **下载网络问题**:国内直连 Mojang 很慢,必须设计镜像切换(优先 BMCLAPI)。
2. **AI 费用**:DeepSeek 极便宜,但仍需一个 API key;可让朋友填自己的 key。
3. **打包与杀软**:PyInstaller 打的 exe 常被 Windows Defender 误报,需要处理(加签名或引导用户添加信任)。
4. **范围失控**:AI 功能诱惑多,时刻守住"先让游戏能跑起来"。

## 7. 灵感清单(随时记录,按需实现)

> 这里攒着"以后想做"的想法,不一定要现在做;实现时优先挑对用户最有价值、改动最小的。

### 灵感 #1:标记"模组生态黄金版本" 🏅
- **想法**:在每个大版本里,给模组生态最好的正式版打上标记,让玩家一眼认出。
  例如 `1.7.xx` → 标记 `1.7.10`;类似还有 `1.12.2`、`1.16.5`、`1.20.1` 等经典模组版本。
- **现状**:已记录(2026 年 2 月,来源:用户灵感)。
- **实现思路(两种,后者更酷)**:
  1. 简单版:人工维护一份"黄金版本"清单(如 `{"1.7": "1.7.10", "1.12": "1.12.2", ...}`),渲染列表时给对应版本加个 🏅 前缀或高亮。
  2. 数据版:接入 **Modrinth API**,按游戏版本统计 Mod 数量,数据说话——哪个版本 Mod 多,生态就好。与阶段 3(Modrinth 搜索安装)共用同一套 API 技术,一举两得。
- **建议时机**:阶段 3(Mod 管理)做完后,顺手把"数据版"实现出来。

### 灵感 #2:联机方案中心 🌐
- **想法**:在启动器里做一个"联机方案"专区,根据玩家的场景(朋友都在同一 WiFi / 分布在全国 / 想开服给更多人)推荐对应的联机方案:
  - **虚拟局域网**(最适合朋友间联机):EasyTier(类似 PCL 的方案)、Radmin LAN、ZeroTier——大家组进同一个虚拟网,像在同一个 WiFi 下联机
  - **内网穿透**(适合想开服但没公网 IP):SakuraFrp、frp——把家里的服务器"穿"到公网
  - **联机模组**:简化联机流程的 Mod(如 Essential 类)
  - **官方方案**:Mojang 在 MC26 提供的官方联机渠道
- **现状**:已记录(2026 年 2 月,来源:用户灵感)。
- **远景(真正值钱的部分)**:不只是"展示方案",而是**整合进启动器和 Agent 的自动配置**——比如用户选"和朋友联机",Agent 自动判断场景、给出步骤、甚至帮你把 EasyTier 装好配好、启动器一键加入朋友的虚拟网房间。
- **建议时机**:先在"灵感清单"里细化每种方案的适用场景和接入成本;等到阶段 2(Agent)和阶段 5(整合)之后再逐项落地。注意每种方案都有自己的协议/客户端,接入时要逐个评估工作量。

### 灵感 #3:NeoForge 支持
- **想法**:1.21+ 的 Forge 基本停更,NeoForge 是主流;用户明确提出需要。
- **现状**:✅ **已实现**(2026 年 8 月)。接口探明:`maven.neoforged.net/releases/net/neoforged/neoforge/<ver>/neoforge-<ver>-installer.jar`(之前 404 是 HEAD 探测被拒,GET 正常)。installer 结构与 Forge 同款(install_profile.json + version.json),复用同一套安装管线;启动用模块路径(`-p` + `--add-modules`),classpath 经 `-DlegacyClassPath` 传给 BootstrapLauncher,已处理。
- **遗留**:NeoForge 的整合包/依赖生态细节待实测。

### 灵感 #4:Mod 兼容性提示 ⚠️
- **想法**:下载 Mod 时,提示该 Mod 与当前实例可能不兼容(如 Mod A 需要 Mod B、与 Mod C 冲突、或作者声明不支持某版本)。
- **现状**:已记录(2026 年 2 月,来源:用户)。Modrinth API 本身提供 dependencies(依赖关系),"潜在不兼容"可能需要结合社区讨论/issue 信息,属于数据来源问题。
- **建议时机**:Mod 下载功能稳定后,先做"依赖自动提示"(Modrinth 有数据),再探索社区信息源。

### 灵感 #5:Mod 关系网状图 🕸️
- **想法**:像 MC百科那样,把 Mod 之间的依赖/前置/冲突关系画成网状图,装整合包时一目了然。
- **现状**:已记录(2026 年 2 月,来源:用户)。数据源用 Modrinth 的 dependencies 字段;画图可用 Qt 的 QGraphicsScene 或 Web 可视化。
- **建议时机**:灵感 #4(依赖提示)之后,作为进阶可视化。

### 灵感 #6:备份机制(防坏档)🛡️
- **想法**:在执行"需要进入世界测试"的操作(装 Mod 后测试、装整合包、更新 Mod 版本等)之前,自动备份:
  1. **游戏存档 saves** → 打包成 `.zip`,防止坏档/Mod 冲突毁档
  2. **模组列表** → 导出为一个 `.txt`(记录当前装了哪些 Mod 及版本,方便回滚对照)
  3. **涉及整合包的操作** → 额外备份 `config` 等配置文件
- **现状**:已记录(2026 年 2 月,来源:用户)。**部分已实现**:CLI `backup <实例>` + AI 装 Mod 前自动备份(2026 年 8 月)。
- **实现思路**:备份目录如 `.minecraft/backups/<实例名>/<时间戳>/`;`zipfile` 打存档包;模组列表 txt 直接扫 mods 目录文件名;**与 AI 权限系统联动**——AI/操作执行"工作区可写"动作前,强制先走一次备份(与 ai_actions.py 的权限检查同层)。
- **建议时机**:补 GUI 备份按钮 + 整合包操作前自动备份;做"AI 自动测试"类功能前(是它的安全前置)。

### 灵感 #7:做教程 📖
- **想法**:给启动器做使用教程/新手引导——面向朋友和社区,降低上手门槛。
- **现状**:已记录(2026 年 8 月,来源:用户)。
- **内容方向**:
  1. **内置引导**:首次启动的引导页(选版本 → 装加载器 → 启动,照着点一遍)
  2. **功能说明**:下载新实例向导、Mod 下载二级菜单、AI 助手(含权限说明、工具自测)
  3. **常见问题**:杀软误报处理、离线账号说明、存档备份/回滚
- **形式候选**:内置"教程"页(可选)/ README 详细版 / 给朋友的口述要点。
- **建议时机**:功能稳定后;发朋友前必做(减少 80% 的"怎么用"答疑)。

### 灵感 #8:跨平台 🌍
- **想法**:Windows / macOS / Linux 都能跑——朋友们的系统不一,只发 Windows exe 会挡掉一半人。
- **现状**:已记录(2026 年 8 月,来源:用户需求)。
- **技术基础**:Python + PySide6 本身跨平台,核心逻辑(下载/启动/加载器安装/AI)几乎没有系统依赖;
  目前需要处理的 Windows 专属点:
  1. `os.startfile()`(打开目录)——换成 `QDesktopServices.openUrl(QUrl.fromLocalFile())`(Qt 跨平台)
  2. 路径一律用 `os.path.join`,不写死反斜杠
  3. Java 查找/运行时:JRE 目录结构一致,但系统 Java 路径探测需按平台分支
  4. 打包:Windows → PyInstaller exe;macOS → .app;Linux → AppImage
- **建议时机**:功能稳定后集中处理;优先改 `os.startfile`,其余大多已天然跨平台。

### 灵感 #9:dry-run 机制 🧪
- **想法**:AI/启动器执行任何"有副作用的操作"(装 Mod、改配置、装整合包、备份等)之前,先提供"演练"模式:预览这次操作会改哪些文件、下载什么、多大、装到哪,用户确认后才真正执行。
- **现状**:已记录(2026 年 8 月,来源:用户)。
- **应用场景**:
  1. AI 工具调用:执行写操作前,先让 AI 输出"将要执行的操作清单",用户点确认再动手
  2. 装 Mod/整合包:先显示将下载的文件列表 + 目标目录 + 大小,再下载
  3. 改 FTB Backups 配置等:先展示将写入的配置内容
- **实现思路**:写操作统一走一个 `dry_run` 开关;工具层在执行前生成"变更描述",GUI 弹确认框。与灵感 #6 备份机制互补(先备份 → 再演练 → 再执行)。
- **建议时机**:AI 权限系统稳定后,作为"工作区可写"的进阶保险。

### 灵感 #10:自制"指令桥"mod(游戏指令终极方案)🧩
- **想法**:自制一个多平台 mod,进世界自动开启 RCON(连"对局域网开放"都不需要),
  并用自定义 `CommandSource` 把命令执行结果**写回文件**——启动器 100% 准确获取游戏指令反馈。
  先例参考:FTB Quests(功能内嵌 mod + 配置文件交互)、Carpet(自带 RCON 实现)。
- **现状**:已记录(2026 年 8 月,来源:用户,论证后确认)。启动器侧已就绪:RCON 客户端、
  server.properties 自动配置、日志增量反馈(`game_command.py`);旧方案(手动 RCON + 模拟按键)
  入口已隐藏,等待 mod 通道替代。
- **方案论证(2026 年 8 月)**:
  1. **不依赖加载器的方案 = Java Agent(-javaagent + Instrumentation 字节码改写)**:
     技术上能 hook 集成服务器启动、反射开 RCON,但**每个 MC 版本类结构都要适配、脆弱、没有
     生态支撑**,做不了"JEI 数据导出/指令结果回写"这类高级功能 → **放弃**。
  2. **多平台 mod(Fabric + Forge + NeoForge)**:成熟生态、版本适配有社区模板(MultiLoader),
     能直接拿 `RecipeManager`/注册表数据、挂事件钩子 → **采用**。
- **mod 功能路线图(未来频繁迭代)**:
  1. **指令桥**:进世界自动开 RCON + 命令结果写回文件(启动器读文件拿精确反馈)
  2. **JEI/配方数据导出**:把 `RecipeManager` 配方 + 物品属性 dump 成 JSON →
     启动器侧做套娃合成计算、物品参数比较(挖掘等级/武器伤害/护甲),AI 据此指导合成
  3. **指令库同步**:mod 侧指令/自定义 NBT 模板与启动器指令中心互通
  4. **未来**:更多启动器相关功能(游戏内状态回传、自动化操作等)
- **项目结构**:工作区 `bridge-mod/` 目录(MultiLoader 骨架,common 核心 + 各平台入口);
  数据导出到实例目录 `.bridge/`(如 `.bridge/recipes.json`、`.bridge/items.json`)。
- **构建环境(2026-08 已验证)**:✅ **fabric 平台编译通过**(产物 `bridge-mod/fabric/build/libs/fabric-0.1.0.jar`)。
  JAVA_HOME = java-runtime-delta(Java 21);Gradle 必须用 **8.10.x**(8.13/8.14 移除了
  `Problems.forNamespace` API 导致 Loom 1.6 崩溃,已踩坑记录在 README)。
  common 源码并入 fabric 编译单元;forge/neoforge 插件版本待适配后加入。
- **兼容目标(2026-08 定)**:**
  **策略:先兼容 mod 生态活跃/多的版本**(不铺开所有版本):
  ✅ Fabric 1.21.1 / ✅ NeoForge 1.21.1(均已编译发布,jar 在 v0.1.0);
  🎯 目标 **1.20.1**(老版本 mod 生态最活跃)与 **1.21.4**(1.21 系列主流);
  ⏳ Forge(1.19 及以前)晚点再说。
  参考方案已论证:Architectury(结构思想已吸收,不引入——其 fabric 实现依赖 fabric-api 会倒退);
  Sinytra Connector(Fabric→Forge 兼容层,可作快速验证,但依赖第三方)。
- **测试阶段声明(2026-08)**:启动器与 bridge-mod 均处于**测试阶段**,功能频繁迭代、可能有 bug;
  建议开发者/尝鲜使用,暂不向普通朋友正式分发。**正版登录**计划正式版(v1.0)加入(未来有可能)。
- **老版本兼容(1.7.10 等)**:单项目同时支持 1.7.10→1.21 不现实(工具链/加载器/API 全不同:
  Fabric 不支持 1.14 前,NeoForge 仅 1.20.1+,老版本只有老 Forge,数据导出 API 是 1.20.5+ 才有)。
  现实路径:现代版本用本 mod;老版本沿用"Lan Server Properties + 模拟按键 + 日志反馈",
  如未来重视老版本再单独建老工具链分支(功能打折)。

### 灵感 #11:物品数据"注册表直取" + 老版本兼容需求(记录)📦
- **关键认知(2026 年 8 月,用户洞察)**:物品管理功能(武器伤害/护甲/韧性/攻速/食物/
  挖掘等级比较、配方套娃合成)的数据源**本质都在游戏注册表 + RecipeManager 里**,
  **不需要 mod 参与逻辑**——mod 只是"导出管道"(把游戏内存数据 dump 成 JSON 给启动器)。
- **推论**:
  1. 真正离不开 mod 的只有**指令桥**(本地 TCP 指令口)和**命令结果反馈**(CommandSource);
     物品/配方导出只是"读注册表写文件"的薄壳。
  2. 未来若不想要 mod 也能拿物品数据:解析游戏目录的 data/resource JSON
     (datapack + 资源,覆盖**原版**物品;代价是**看不到 mod 物品**),指令桥仍绕不开 mod。
- **老版本兼容需求(待触发)**:当出现"需要兼容老版本(如 1.7.10 / 1.12.2)"的实际需求时,
  按此评估:
  1. 老版本加载器只有老 Forge(无 Fabric<1.14、无 NeoForge);
  2. 老版本注册表 API 完全不同(无 BuiltInRegistries / DataComponents / Holder,
     属性在 Item / ItemStack 直接方法),导出代码要按版本重写;
  3. 轻量替代:原版物品走 datapack/资源解析 + 指令桥用现成 RCON mod(如 Lan Server Properties
     的老版本对应物),完全不需要自制 mod;
  4. 只有"批量自动化指令/精确反馈"成为硬需求时,才值得为老版本建老工具链分支
     (JDK 8 + 老 ForgeGradle + 老 Gradle)。
- **现状**:已记录(2026 年 8 月,来源:用户)。当前不实现,需求触发时按上评估。

### 灵感 #12:bridge-mod 分发策略(已定)📦
- **决策(2026 年 8 月,与用户确认)**:❌ 不做"启动器内置源码自动编译"——
  朋友电脑没有 JDK/Gradle/依赖缓存,每次装 mod 拉工具链编译不可行。
  采用:**预编译 jar + 双通道分发**。
- **开发/发布流**:本地编译(fabric 已通;forge/neoforge 待适配)→ jar 上传
  **Modrinth 发布(主,规范+版本管理+玩家可手动装)+ GitHub Releases(备/镜像)**。
- **分发通道**:
  1. **离线通道**:三平台 jar 打进启动器 exe(PyInstaller datas),
     "一键配置"装桥 mod = 从 exe 内置资源复制到实例 mods → 零联网无感
  2. **在线通道**:启动器「检查桥 mod 更新」从 Modrinth/GitHub 拉新 jar
     (复用现成 download_mod 链路,数据源换成我们的项目)
- **版本管理**:启动器内置"桥 mod 版本表"{loader: {mc版本: {版本, URL}}};
  迭代 = 编译新 jar → 发布 → 更新版本表。
- **现状**:策略已定,待 forge/neoforge 平台可编译后落地发布。
- **建议时机**:现方案(Lan Server Properties + 日志反馈)够用时并行开发 mod;
  mod 每个功能独立交付,不影响启动器现有功能。

## 8. 技术调研记录(2026 年 2 月)

### PCL 的中文搜索怎么做的(源码调研结论)
- PCL **自带本地 Wiki 数据库**(`WikiEntries.txt`,约 2.9 万条):Modrinth/CurseForge **slug ↔ MC百科中文名** 映射,附带浏览量排行。
- 搜索:中文关键词 → 查本地库命中中文名 → 拿 slug 去平台 API 取详情;英文直接搜平台。
- 展示用 `TranslatedName`(库里有中文就用中文);下载文件名可自动加中文前缀(如 `【工业时代2】...jar`)。
- PCL 用 `mod.mcimirror.top` 镜像代理 Modrinth/CurseForge API + CDN(国内加速)。
- **我们的实现**:`mod_cn.py` 本地中文名库(起步 60+ 个,随用随加)+ `search_mods_cn` 中文搜索逻辑。已实现 ✅

### MC百科 API 现状
- **没有官方公开 API**。
- 第三方:[TixXin/MCMod-Search-API](https://github.com/TixXin/MCMod-Search-API)(自建 PHP 爬虫,需自己部署)、MCP server(chartyr-mcmod_wiki_mcp,给 AI 用)。
- **结论**:不在启动器里直连 MC百科;中文名走本地库(参考 PCL),MC百科页面链接仅作跳转。
