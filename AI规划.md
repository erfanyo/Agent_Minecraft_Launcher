# AI 模块规划(草稿 v1)

> 这是一份"活文档",随着讨论不断更新。
> 创建日期:2026-08-23
> 关联文档:项目规划.md(总体规划)、ROADMAP.md(路线图)
>
> 📌 **续接入口(2026-08-23 ②原型进行中)**:下一步工作是"重大更新"——
> 1. ✅ **§8.1 模型验证已完成**:下载并实测 Qwen3.5-0.8B 通用版 vs Function-Calling(xLAM)微调版
>    (均 Q4_K_M,31 条回归测试集 ×3 次贪心解码),**xLAM 微调版胜出**(综合 71.8% vs 51.6%),
>    最终模型 = `qwen3.5-0.8b-function-calling-xlam.q4_k_m.gguf`(详见 §8.1)
> 2. ✅ **本地推理模块原型(grammar 约束解码)已打通**:`local_ai.py` GrammarToolEngine,
>    实测参数准确率 +9.6%(79.0% vs 69.4%,§8.2 已更新)
> 3. ✅ **任务路由已实现**:`task_router.py`(难度判定 + FAQ 规则引擎 + 失败链路,§1 落地)
>    - schema 单一来源:local_ai 从 assistant.TOOLS 自动生成 GBNF,加工具只改一处
> 4. ✅ **§8.1 已知问题已修复**(ask_user 架构层强制 / compare_items 英文别名 / 启动实例误解,
>    修复后全量回归 78.5%,见 §8.1)
> 5. ✅ **前端接入已完成(2026-08-23)**:路由与聊天循环接入(W3)、模型懒加载 + 自动下载 +
>    进度弹窗、状态徽标(未下载/下载中/已就绪/推理中)、多模态按 provider 联动、`ai_in_game`
>    三档 + 游戏启动卸载/保留、窗口关闭卸载无残留、冷启动预热 —— 任务书 W1-W7 全完成
>    (落地记录见 `_agent_comms/任务书-本地模型前端接入.md` §8/§9;commit c0a4943/037af38)
> 6. 继续讨论任何未决项(见 §8)

## 0. 核心定位

**目标:让萌新开箱即用,不注册 API 也能体验基础 AI 功能;API 密钥是可选升级,不是使用前提。**

核心架构一句话:**云端大模型当"大脑"(规划/拆解/复杂任务),本地小模型当"手"(执行简单任务)** —— planner / executor 分层。

小模型强项:分类、抽取、槽位填充、翻译、摘要。
小模型弱项:规划、多步推理、诊断 —— 这些交给云端。

> ⭐ **最看重的单项能力:工具调用(function calling)** —— 选型时以此为首要标准。
> 小模型(1B 以下)的可靠上限是**单次工具调用**(选对函数 + 填对参数),多步工具编排不可靠;
> 启动器场景恰好是"一次调用一个动作"(改内存、装 mod、查日志),匹配良好。

---

## 1. 任务路由与难度判定

### 1.1 路由原则

| 任务类型 | 执行方 |
|---|---|
| 简单任务(指令解析、分类、抽取、翻译、摘要) | 本地小模型 |
| 困难任务(诊断、代码分析、多步规划) | 云端大模型 |
| 规则引擎可覆盖的固定问答(FAQ) | 规则引擎(最稳、零成本) |

> ✅ 已实现:`task_router.py`(2026-08-23)
> - 优先级:含工具动词的指令 → 本地;纯 FAQ 问答 → 规则引擎;困难任务 → 云端;未知 → 云端兜底
> - 失败链路 `run_with_fallback`:本地 → 规则引擎 → 云端 → 诚实认输,每层失败自动落下一层
> - 已端到端联调:route → local_ai(grammar)→ cloud 桩 → give_up 全链路验证通过
> - 本地选错工具时(如"下载游戏"误选 launch_game),由 executor 执行 + 兜底信号转云端(§1.2)
> - 🔌 **已接入聊天循环(W3,2026-08-23)**:`assistant.py` `AIChatDock` 新增
>   `_local_enabled`(provider=local_builtin 时启用)/ `_get_local_engine`(懒加载单例)/
>   `_local_model_ready`(模型已下载)/ `_local_tool_call`(grammar 调用 + executor 执行);
>   `send()` 的 worker 按 `route()` 分流:rule 直接答 / local 走本地(失败落云端,§1.4)/
>   其余走云端 chat_with_tools(未动)。默认 provider=deepseek,本地路径不启用,不影响现有行为

> 📌 **「本地能力不足 → 自动切云端」的具体规则**(2026-08-23,与前端设置文案对齐):
> 来源:`task_router.route(text, have_cloud, have_local)`,按优先级判定:
> 1. **歧义**(推荐 / 该装哪些)→ 走 `ask_user` 先问用户,不让模型瞎猜;
> 2. **含工具动词**的指令(装 / 查 / 改 / 启动)→ 本地小模型;但**同时属困难任务**(诊断 / 代码 / 多步规划)→ 自动转云端;
> 3. **命中 FAQ**(怎么下载 / 联机 / Java)→ 规则引擎直接答(最稳、零成本);
> 4. **困难任务**(深度诊断 / 代码分析 / 多步规划 / 方案)→ 云端;
> 5. **未知难度**(如"今天天气")→ 云端兜底。
> 失败链路(§1.4 `run_with_fallback`):本地失败 / 没选工具 / 超时 → **自动落云端**;全失败 → 诚实认输。
> AI 设置页「本地模型」组的"内置本地模型 + 规则引擎怎么工作?"说明文案即按此编写
> (`assistant.py` `AISettingsForm.local_explainer`)。

> ⚠️ **已知风险(2026-08-23 记录,先不改,遇到问题再改规则)**:
> 规则引擎(RULE_FAQ 关键词匹配)可能被"小白发言"钻漏洞:
> - 小白常用口语不精确,可能命中 FAQ 宽泛关键词(如"怎么弄"/"内存"/"java"/"mod"),
>   得到"看似答了其实没解决"的回答,而不走正确的工具/模型路径
> - 例:用户说"我内存好卡怎么办"可能命中"内存"FAQ 得到配置建议,但其实想诊断卡顿(应走云端诊断);
>   "java 报错"同理
> - 触发信号:用户对 FAQ 回答继续追问(说明没答到点子上)
> - 缓解方向(以后再改,现在记录):① FAQ 关键词收紧 + 要求"含动词/问句才匹配";
>   ② 命中 FAQ 但用户追问 → 降级走模型;③ 反馈闭环(§1.5)数据反哺调规则

### 1.2 难度判定(不用 token 数)

> 原始想法:用 token 数衡量任务难度。
> 结论:**token 数与难度相关性弱**(短句可能是难题,长文可能是机械任务),弃用。
> 且小模型有"过度自信"通病,自评不可靠。改用**结构化信号**:

1. **任务类型难度清单**:诊断 / 代码分析 / 多步规划 = 难;翻译 / 摘要 / 分类 = 易
2. **输出格式匹配**:能填进预设 JSON 槽位 = 本地干,否则转云端
3. **兜底信号**:本地结果置信度低、超时、超长 → 自动转云端

### 1.3 "转云端"的交互包装

- 不能是技术提示("此任务较难,建议切换云端"),要用**业务语言**:
  "这个任务超出了我的基础能力,升级后可以完成,要不要试试?"
- 允许用户选择"就用基础版凑合一下"

### 1.4 失败链路(每一层失败都有下一层,不能断链)

```
本地小模型 → 规则引擎 → 云端大模型 → 诚实认输("我尽力了")
```

### 1.5 反馈闭环

- 每个 AI 结果旁放"有用 / 没用"按钮
- 数据用于调路由阈值和规则引擎 —— 系统越用越聪明的唯一途径

---

## 2. 首次启动:AI 插件化

- 首次启动询问是否安装 AI 插件
- **默认开启**(基础功能无需密钥、不花钱),但模型文件**懒加载**:
  - 插件先装上(几百 KB)
  - 模型等用户**第一次真正用到 AI 功能时**才后台下载
  - 下载完成前用规则引擎顶替
- 智能检测:环境变量 / 配置文件里已有密钥 → 自动识别并测试连通性

> 📌 **多模态 UI 目标(2026-08-23 更新)**:看模型设置,**所用模型不支持多模态就隐藏图片相关功能**。
> - 机制已存在:`ai_multimodal` 设置 + `update_vision_ui()`(显示/隐藏 📷/🖼 按钮、清待发图片)
> - 本次只加"按 provider 自动预设":`local_builtin` → False(本地 xLAM 纯文本,已确认无视觉头);
>   `openrouter` → 可默认 True;其余保持手动;勾选框允许用户覆盖
> - 前端实施见任务书 `任务书-本地模型前端接入.md` W2

---

## 3. Mod 翻译模块

- 场景:mod 选择 / 详情界面,本地小模型做翻译
- **术语表注入**:MC 标准译名(下界、末地、附魔、刷怪笼…)进 system prompt,提升专有名词翻译质量
- **翻译缓存**:同一 mod 只翻一次,落盘缓存;缓存**绑定模型版本号**,模型更新即作废
- **置信度标记**:低置信度翻译标注"机翻仅供参考"
- **避开游戏时段**:批量翻译排队到游戏运行之外执行
- **人设联动**:绿袍智者说"这段原文有点绕,我翻得可能不准,但大意是……"

---

## 4. 引导模块:一次性向导 + 常驻小助手(并行)

### 4.1 并行设计

- **向导**:首次启动只介绍最核心的三件事;可跳过、可重开(设置里常驻"重新引导")
- **常驻助手**:"不懂再问",按需出现
- **关键前提**:助手必须知道用户进度状态(已填密钥?已装插件?),答问前先看状态,避免重复教学

### 4.2 引导内容 = 易腐数据,不是代码

- 写成 **.md + 标注日期**(人和机器都能读,可进 git diff)
- **YAML frontmatter**:`date`、`version`、`applies_to`(适用启动器版本)、`checksum`
- **过期判定**:超过阈值(如 90 天)显示"此说明可能已过期",并尝试拉取新版;拉不到就用旧版 + 过期标记
- **服务端下发 + 热更新**,跟随外部世界变化,不跟启动器发版

### 4.3 对抗注册界面改版的关键思路

**验证最终结果,而非描述流程。**

- ❌ 教用户"点这里、填那里" → 会被页面改版打死
- ✅ "去注册一个密钥,粘回来,我帮你检查能不能用" → 启动器端到端校验密钥有效性
- 引导外部世界永远会过期,校验内部结果永远不过期;版本号是第二道防线

### 4.4 分级引导(同一套内容,三个详略等级)

| 等级 | 面向 | 人设 |
|---|---|---|
| 萌新 | 看不懂任何术语 | 绿袍智者(傻子村民也看得懂) |
| 进阶 | 想看参数细节 | 图书管理员 |
| 开发者 | 完整文档 | 普通村民 |

### 4.5 失败路径设计(最容易被忽略)

密钥填错、额度用完、云端服务挂了 —— 每一条失败路径都要有**友好的兜底文案**,而不是弹红框报错。萌新第一次体验的崩塌往往发生在失败路径上。

### 4.6 进度勋章(已写入规划)

- 完成注册引导 = 「图书管理员」成就
- 填好密钥 = 「附魔师」
- 触发性能提示 = 「别在岩浆旁玩电脑」
- 小成本高粘性

### 4.7 数据驱动迭代

- **匿名漏斗埋点**:哪一步用户卡最久、跳过率最高 → 决定引导内容怎么改
- **离线降级**:拉不到最新引导内容 → 显示缓存版本并标注"此页面可能已更新"

### 4.8 答问知识库分层

```
FAQ 模糊匹配(规则引擎) → 小模型 → 云端
```

规则引擎处理固定问答最稳,匹配不到再上模型。

---

## 5. 性能策略(LLM 运行时的游戏影响)

| 策略 | 说明 |
|---|---|
| 量化提示 | 显示"当前模型推理占用 CPU 约 X%",用户有判断依据 |
| 主动避让 | 检测到游戏进程启动 / 聚焦 → 推理暂停或降优先级;游戏进程优先级始终高于推理 |
| 用完即卸 | 任务完成后立刻卸载模型,回到几百 KB 空闲态(常驻内存是最隐蔽的性能杀手) |
| 温控意识 | 笔记本过热降频,高温时劝退重任务 |
| 冷启动策略 | 借游戏启动加载的空闲时间预加载模型 → AI 功能"秒开"(待验证) |

### 5.1 游戏启动时模型去留(2026-08-23 决策)

> 背景:bridge-mod 未来提供**游戏内 AI 交互入口**(聊天栏/快捷键向启动器提问并回显,日程已记)。
> 模型是否随游戏启动卸载,取决于"游戏内 AI 是否要用本地模型",不能一刀切。

**用设置项 `ai_in_game`(游戏内 AI 通道)决定卸载策略**,三档:

| ai_in_game | 含义 | 游戏启动时的卸载策略 | 适用 |
|---|---|---|---|
| `off`(默认) | 游戏内不用 AI | 卸载本地模型(§5 用完即卸+主动避让,省内存给游戏) | 大多数玩家,游戏内不需要辅助 |
| `cloud` | 游戏内 AI 走云端 | 卸载本地模型(游戏内走云端,本地模型不参与,省内存) | 游戏内辅助复杂、想用更强模型 |
| `local` | 游戏内 AI 走本地模型 | **不卸载**,模型常驻服务游戏内请求;但要:降推理优先级 + 内存监控(游戏+模型共存) | 离线玩家,游戏内轻量辅助 |

**联动逻辑(游戏启动事件触发)**:
- `off` / `cloud` → `engine.stop()`(卸载,回到几百 KB 空闲态)
- `local` → 保持加载;推理线程降优先级;若系统内存吃紧(游戏申请内存失败风险)→ 提示用户或临时卸
- 游戏退出 → 按 `ai_local_auto_download`/空闲策略决定是否重新加载(冷启动预加载待验证)

**设置界面**(前端任务书 W7):
- 新增设置项:游戏内 AI 通道:关闭 / 云端 / 本地模型
- 文案示例:"游戏内 AI 辅助(需要 bridge-mod 支持):关闭 / 云端(更强,需联网) / 本地模型(离线)"
- 该设置存 `config.json` → `ai_in_game`;前端只做设置项 + 触发游戏启动/退出事件时调用
  后端提供的 `local_engine.stop()` / `local_engine.start()`(后端已具备,前端接线)

---

## 6. 版本化矩阵(四个独立维度)

| 维度 | 跟随什么变化 |
|---|---|
| 引导内容版本 | 外部世界(注册页面、流程) |
| 启动器版本 | 启动器发版 |
| 云端 API 契约版本 | 服务端接口 |
| 模型版本 | 模型更新 |

> 考虑统一资源清单文件管理所有可更新资源(模型、规则集、术语表、引导),
> 所有更新走同一套机制,避免维护多套更新逻辑。

---

## 7. 本地 AI 技术要点

1. **grammar 约束解码(最优先)**:llama.cpp GBNF 语法强制结构化输出,小模型只能吐出合法 JSON 且字段齐全 —— 从根上解决"小模型不听话"的格式问题
2. **安全:mod 简介是不可信内容**:防 prompt injection,mod 内容永远只作为"待处理数据"传入,与系统指令严格分离,任何从 mod 内容提取的"指令"一律忽略(架构层面定死)
3. **本地知识注入**:把已装 mod 清单、游戏版本、最近崩溃日志作为上下文喂给模型(最轻量 RAG)
   > ✅ 已实现(2026-08-23):`local_ai.py` `tool_call(text, context=...)` + `build_launcher_context()`
   > (真实实例清单 + 设置)。实测注入上下文综合 73.9% vs 无上下文 66.9%(净提升 +7.0pp)。
4. **回归测试集**:固定测试用例(几十条典型指令 + 期望输出),模型 / 规则更新后自动跑,防"修好一个弄坏三个"
   > ✅ 已实现:`python local_ai.py --regress`(31 条 ×3 次平均 + 注入真实上下文,2026-08-23)
5. **模型文件校验**:下载时 sha256 校验,防损坏 / 中间人替换;版本钉住
6. **数据分级与隐私**:玩家名、账户信息、世界内容绝不离开本地;边界写进隐私说明(信任感来源)

---

## 8. 待定事项(先记着,延后决策)

### 8.1 模型选型 ✅ 已实测拍板(2026-08-23)

> 讨论结论(2026-08-23):**本地只养一个模型通吃**,不做多模型分场景调度(调度开销大、收益小)。
> 分层为:本地单模型(简单任务)+ 云端大模型(难活)+ 规则引擎(琐碎活),三层不多不少。

**最终模型:xLAM 微调版 `qwen3.5-0.8b-function-calling-xlam.q4_k_m.gguf`** ⭐

**实测对比(2026-08-23,31 条回归测试集 ×3 次贪心解码,LM Studio llama.cpp CUDA,均 Q4_K_M)**:

| 模型 | 工具名准确率 | 参数准确率 | 综合得分 |
|---|---|---|---|
| 通用版 `Qwen3.5-0.8B-q4_k_m` | 51.6% | 51.6% | 51.6% |
| **xLAM 微调版** | **74.2%** | **69.4%** | **71.8%** |

- xLAM 专为函数调用训练,工具选择与参数填充全面胜出,符合"专才>通才"预期
- 两版共性问题(原型阶段要处理):ask_user 触发不灵(倾向直接猜工具)、
  compare_items 参数填英文(damage/armor,工具描述需加别名)、"启动实例"被误解为 install_instance
- 测试集:`ai_testset.py`(31 条);评测脚本 `.tmp/eval_models.py`(走 LM Studio API,温度 0 可复现);
  报告 `.tmp/eval_out/compare.md`;模型文件在 `AMCL/models/`(manifest.json 资源清单 + sha256 钉住)
- 模型下载源:hf-mirror 优先 + HF 兜底(2026-08-23 直连可用)

**已修复(2026-08-23,修复后 grammar 全量回归 78.5% vs 修复前 76.6%,无回归)**:
1. **ask_user 触发不灵** → 架构层强制:`task_router` 检测歧义请求(推荐/该装哪些/你决定)
   直接构造 ask_user 调用(问题 + 候选选项),不依赖小模型自觉(0.8B 指令遵循弱,预警属实)
2. **compare_items 英文参数** → 双保险:工具描述强化"必须用中文" + `recipe_graph.compare_items`
   增加英文别名映射(damage/armor/toughness/speed,大小写/空格容错),模型填英文也能查
3. **"启动实例"误解** → 描述强化区分:"创建新实例(install_instance)" vs "启动已有实例(launch_game)",
   触发语境写进描述;云端 assistant.TOOLS 与本地 TOOL_DESCRIPTIONS 同步更新

**候选模型:Qwen3.5-0.8B,量化 Q4_K_M** ⭐(用户推荐,认可)

- 0.6B 指 Qwen3-0.6B(上一代最小款);Qwen3.5-0.8B 已发布,代际收益 > 参数差距
- **量化依据**:实测研究表明中度量化(Q4/Q5)对工具调用破坏很小,Q4_K_M 是体积与工具调用可靠性的甜点;过低量化(Q2/Q3)会明显破坏函数选择与 JSON 输出
- **硬性配套**:grammar 约束解码(§7.1)不能省 —— 量化是"概率上更稳",grammar 是"结构上必对"
- 外部分数(BFCL 等)仅供参考,启动器场景(自家工具集 + 指令风格)是唯一标准(本次实测即为自家场景)

**已否决/延后项**:

- 分场景专用小模型(0.1B 分类器 + 大模型翻译)❌ 现阶段否决:调度复杂,收益在启动器场景不可感知;触发条件 = 出现"每操作都触发模型"的高频路径(暂不存在)
- 硬件自适应量化档位(轻量/均衡/高质量)⏳ 仍延后,等真实使用数据
- 模型文件格式(GGUF)、mmap 加载策略 ✅ 确定走 llama.cpp 生态

### 8.2 待验证项

- grammar 约束解码在小模型上的实际效果 ✅ 已实测(2026-08-23)
  - **结论:grammar 显著提升参数准确率,验证了"结构上必对"**
  - 实测(xLAM Q4_K_M,31 条测试集 ×3 次平均):grammar 约束 参数 79.0% vs LM Studio 原生 tools 69.4%(+9.6%);工具名 74.2% 持平
  - grammar 主战场是**参数填充完整性**(required 字段强制必填);工具选型仍是提示工程的事
  - 技术要点(踩坑记录,原型复用):
    - GBNF 规则名**不允许下划线**(llama.cpp 限制,用 args0/args1...)
    - 每个工具一个 root 分支,把 name 字面量与对应参数结构**绑定**,否则 name/args 独立选择会错配
    - 模型在必填参数"不想填"时会卡住狂吐空格直到 token 上限 → 需提示"补全必填参数"+ 解析失败自动重试
    - grammar 只作用于 llama.cpp 原生 /completion 端点;LM Studio 的 OpenAI 兼容层对推理模型不生效
  - 原型模块:`local_ai.py`(GrammarToolEngine,自动从工具 schema 生成 GBNF)
- 冷启动预加载是否值得(内存 vs 延迟的取舍)

---

## 9. 当前测试阶段的密钥管理(临时措施)

- 现状:一个共享密钥给朋友测试(用户已知悉不安全)
- 风险:账单风险、权限不可控(无法区分谁在用什么)、泄露只是时间问题
- 缓解建议:
  - OpenRouter 子密钥(每人一个,独立额度上限)
  - 或极薄代理(限额转发),单个人滥用只烧自己的配额
  - 平台消费告警必须开
- 正式版:用户自带密钥 + 开箱即用的本地基础功能

---

## 10. 决策记录

| # | 决策 | 结论 |
|---|---|---|
| 1 | 难度衡量 | ❌ token 数 → ✅ 结构化信号(任务类型清单 + 输出格式匹配 + 置信度/超时兜底) |
| 2 | 插件默认状态 | 默认开启,模型懒加载,规则引擎顶替 |
| 3 | 引导形式 | 一次性向导 + 常驻助手并行,助手感知用户进度 |
| 4 | 引导内容载体 | .md + 日期 + YAML frontmatter,服务端下发热更新 |
| 5 | 对抗页面改版 | 验证最终结果,而非描述流程 |
| 6 | 性能 | 提示量化 + 主动避让 + 用完即卸 + 温控 |
| 7 | 模型选型 | ✅ 已拍板:xLAM 微调版 Qwen3.5-0.8B Q4_K_M(2026-08-23 实测:综合 71.8% vs 通用版 51.6%) |
| 8 | 结构化输出 | grammar 约束解码(最优先落地) |
| 9 | 安全 | mod 内容 = 不可信数据,与系统指令严格分离 |
| 10 | 分场景多模型 | ❌ 现阶段否决,单模型更轻量;触发条件 = 高频模型路径出现 |

---

## 11. 文件放置约定(AMCL 私有数据目录)⭐

> **背景**:`AMCL/` 一开始设计为"保存启动器设置"的文件夹(启动后只写 `config.json`);
> 现在扩展为**启动器私有数据目录** —— 启动器需要下载/生成的一切文件都放进 `AMCL/`
> (或启动器自己的目录树内),**绝不往用户系统目录散落**(`%APPDATA%` / `%TEMP%` /
> 文档 / 桌面等),像很多程序那样在系统里"到处拉屎"是不允许的。便携原则见 `paths.py`。

**AI 部分(已落实)**:

| 路径 | 内容 | 来源模块 |
|---|---|---|
| `AMCL/config.json` | 启动器设置(含 AI 设置、镜像策略、多模态开关) | settings.py |
| `AMCL/models/` | 本地模型 GGUF + `manifest.json`(sha256 钉住) | model_registry.py |
| `AMCL/runtime/llama-cpp/` | llama.cpp 服务器与依赖(本地推理运行时) | local_ai.py |

**其他所需文件,与 AI 部分同一原则**:

- 一切新下载/生成的文件(运行时、缓存、临时文件、清单、日志等)一律放启动器目录内:
  设置/私有数据 → `AMCL/`;游戏数据 → `.minecraft/`;其余 → 启动器目录下的专属子目录。
- 新增功能落地前先问一句"**文件放哪**":答不上来 = 先定目录再写代码,
  禁止默认写用户目录(特别是 `%APPDATA%` 散文件、`%TEMP%` 常驻文件、文档/桌面)。
- 唯一例外:启动器所在目录不可写(如装在 Program Files)时,`paths.py` 已自动整体退回
  `%APPDATA%\AgentMinecraftLauncher` —— 那仍是**一个**专用文件夹,不是散落。

