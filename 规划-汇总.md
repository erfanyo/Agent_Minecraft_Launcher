# Agent Minecraft Launcher — 规划汇总(权威)

> 📌 **以本文件为准**。其余三份为历史拆分文档,仍保留供追溯(见文末「相关拆分文件」):
> `项目规划.md`、`AI规划.md`、`ROADMAP.md`。
> **更新日期**:2026-08-24 · **状态**:方向已定 ✅ / 技术栈已定 ✅ / AI 方案已定 ✅;当前处于**下一版迭代**(2026-08-24 用户目标已完成,见 §1)。
> 文档约定:✅ 已完成 / 🔜 规划中(按需启用)/ 🗓 候选(用户挑选)。

---

## 0. 已确认的决策(记录在案)

| # | 决策 | 结论 |
|---|---|---|
| 0.1 | 产品类型 | AI 助手型启动器,给朋友/社区用 |
| 0.2 | 使用场景 | **原版 + 模组 + 服务器全都要** |
| 0.3 | 技术栈 | **Python + PySide6**(2025 拍板;逻辑与界面分离,未来可换 Web 壳) |
| 0.4 | AI 接入 | **可配置**,默认 DeepSeek API,可选 Ollama 本地模型(OpenAI 兼容接口;本地模型已扩展为内置 provider) |
| 0.5 | 账号 | 先**离线登录**,微软正版登录计划 v1.0(暂缓) |
| 0.6 | AI 分层 | **云端大模型当"大脑"(规划/拆解/复杂任务)+ 本地小模型当"手"(简单任务)+ 规则引擎(琐碎活,最稳零成本)** —— 三层不多不少 |
| 0.7 | 许可 | **GNU AGPL-3.0**(仓库根 `LICENSE`,2026-08-24) |

> 成功标准(给朋友用的底线):打开就能用、崩溃有解释、下载不卡死。
> 核心价值:别人家的启动器只会"启动游戏",我们能**回答问题、帮你诊断报错、自动装 Mod**。

---

## 1. 路线图状态(勾选)

### ✅ 已完成

- [x] **AI 本地模型选型拍板**(§8.1 模型验证,AI规划 §8.1)— 2026-08-23
  - 下载 Qwen3.5-0.8B 通用版 + xLAM 微调版 GGUF(均 Q4_K_M,sha256 校验,存 `AMCL/models/` + `manifest.json`)
  - 回归测试集 `ai_testset.py`(31 条)走 LM Studio llama.cpp(CUDA)实测:**xLAM 微调版胜出(71.8% vs 51.6%)**
  - 最终模型 = `qwen3.5-0.8b-function-calling-xlam.q4_k_m.gguf`
- [x] **配方查询修复 + 套娃展开(A+B+C)** — 2026-08
  - 数据定位 `get_recipe_path(instance)`;完整合成树(每步标机器 + 材料总账向上取整);中文名索引(从 jar 语言文件构建 中文/英文名→id)
- [x] **本地推理模块原型:grammar 约束解码** — 2026-08-23
  - `local_ai.py` GrammarToolEngine:从工具 schema 自动生成 GBNF;实测参数准确率 +9.6%(79.0% vs 69.4%)
- [x] **任务路由与失败链路**(AI规划 §1 落地)— 2026-08-23(`task_router.py`)
- [x] **§8.1 已知问题修复**(ask_user 架构层强制 / compare_items 英文别名 / 启动实例误解,全量回归 78.5%)
- [x] **本地推理模块接入启动器全链路**(W1–W7)— 2026-08-23(前端 provider/多模态/聊天循环/自动下载+进度/游戏内 AI 通道;后端上下文注入 + `--regress`)
- [x] **指令通道职责勘误(2026-08-23)**:bridge-mod 内部整合指令口,不负责开启 RCON;RCON 是长期备用(见 §5)
- [x] **旁路:直接读 mod jar 配方(无需进游戏)** — 2026-08-23(`recipe_datapack.py`,jar 数据为基座 + bridge 数据覆盖)
- [x] **配方"原料未导出"缺口** — 旁路解决了 Mekanism 等特殊配方原料缺口(读不到仍标"需游戏内确认")
- [x] **灵感 #1 标记"模组生态黄金版本"** — 2026-08-24(标记指定版本方式,简单版人工清单 + 数据版走 Modrinth)
- [x] **灵感 #3 NeoForge 支持** — 2026 年 8 月(maven.neoforged.net;installer 结构与 Forge 同款,模块路径启动已处理)
- [x] **Mod 中文搜索支撑**:`mod_cn.py` 本地库 + `search_mods_cn` 中文搜索逻辑(参考 PCL 做法)

> **【本轮 2026-08-24 迭代 · 已落地】**:
> - [x] **资源中心 Mod 列表打开即默认加载**:进入 Mod / 光影包 / 数据包 / 资源包页即展示热门列表,无需先搜索(commit `b75ae36`,`resource_center.py`;空 query 默认浏览 + `maybe_auto_load` + 切换排序刷新)
> - [x] **Mod 中文名对照表**:自建 Modrinth **Top-100** 清单(方案①,无 PCL 许可负担),与 curated `CN_NAMES` 合并后共 **141 条**(commit `f63a8e7` `mod_cn.py` / `mod_cn_ext.json`;`538a22b` `modrinth.py` 标题用 `merged_names()`)
> - [x] **Mod 详情「在 MC百科查看」链接**:详情面板一键用浏览器打开该 Mod 的 mcmod.cn 搜索页(commit `661d558`;仅链接,不爬取/入库)
> - [x] **自动更新修复**:重启不再报 PyInstaller 父进程校验失败(commit `3d7df89`;改用 `explorer.exe` 拉起新 exe)
> - [x] **许可改为 AGPL-3.0 + 文档**:仓库根 `LICENSE`(commit `620cf75`)+ README 徽章 / CHANGELOG 备注(commit `a0a075d`)

### 🔜 规划中(按需启用)

- [ ] **bridge-mod 游戏内 AI 交互入口** — 在 bridge-mod 里提供游戏内 AI 入口(聊天栏/快捷键 → 向启动器提问 → 回显);通道由 `ai_in_game` 决定(off/cloud/local);启动器侧需接收游戏内请求的本地通道(bridge 指令口扩展,后端待定);卸载联动已定(见 §4.2)
- [ ] **D. 配方数据新鲜度** — 返回信息带"数据来自 X 实例、导出于 YYYY-MM-DD HH:MM";换 mod/重装后提示"数据可能过期";可选对 recipe 数量做摘要
- [ ] **崩溃分析与修改意见(进阶)** — 分析层已具备(读日志/崩溃报告 + 云端诊断 + 崩溃守护);待做:① 分析结果 → 结构化"修改意见"清单;② 远期:权限内自主修复回路(与 `ai_actions` 权限联动)
- [ ] **AI 引导/教程落地**(见 §4.3 引导模块设计;灵感 #7)

### 🗓 候选(用户挑选)

- [ ] MCP server(让外部工具/脚本也能调用启动器能力)
- [ ] 26.2 实例回归测试
- [ ] bridge-mod 多版本支持(1.20.1 / 1.21.4)
- [ ] Forge(1.19 及以前)加载器支持
- [ ] RconAutoOpener 反射修复(RCON 长期通道改进:免装 Lan Server Properties 也能自动开 RCON)
- [ ] `check_bridge_mod` 更新流程接入一键配置
- [ ] `ask_user` 回归用例排查(ask_01/ask_02 持续 0 分,历史问题)
- [ ] `route_by_model` 评审结果短时缓存(P2/P3,减少重复本地推理)
- [ ] **界面模式:新手 / 专家**(老玩家少看提示、新玩家多引导)
- [ ] **整合包作者辅助**:导入/导出/编辑 `.mrpack`、依赖检查、本地试装验证、一键打包上传 Modrinth
- [ ] **灵感 #13 宣传视频**(本版 UI + emoji 优化收尾后启动)

---

## 2. AI 模块方向

### 2.1 核心定位

**让萌新开箱即用,不注册 API 也能体验基础 AI 功能;API 密钥是可选升级,不是使用前提。**
小模型强项:分类、抽取、槽位填充、翻译、摘要。小模型弱项:规划、多步推理、诊断——交给云端。
> ⭐ **最看重的单项能力:工具调用(function calling)**;小模型(1B 以下)可靠上限是**单次工具调用**,启动器场景恰是"一次调用一个动作"(改内存/装 mod/查日志),匹配良好。

### 2.2 任务路由与难度判定

路由优先级(AI规划 §1.1):寒暄/介绍 → 本地 chat;含工具动词 → 本地工具;纯 FAQ → 规则引擎;困难 → 云端;未知 → 云端兜底。
- **难度判定**(不用 token 数):任务类型清单(诊断/代码/多步规划=难;翻译/摘要/分类=易)+ 输出格式匹配(能填预设 JSON 槽位=本地)+ 兜底信号(置信度低/超时/超长→云端)。
- **失败链路 `run_with_fallback`**:本地小模型 → 规则引擎 → 云端大模型 → 诚实认输("我尽力了"),每层失败自动落下一层。
- **"转云端"包装**用业务语言:"这个任务超出了我的基础能力,升级后可以完成,要不要试试?"
- **反馈闭环**:每个 AI 结果旁放"有用/没用"按钮,数据用于调路由阈值和规则引擎。
- **已知风险(不急着改)**:规则引擎 FAQ 关键词匹配可能被"小白口语"钻漏洞(如"我内存好卡"命中"内存"FAQ);缓解方向:FAQ 关键词收紧 + 命中 FAQ 但用户追问 → 降级走模型 + 反馈闭环调规则。

### 2.3 首次启动:AI 插件化(引导设计,会话 §4)

- 首次启动问是否装 AI 插件,**默认开启**(基础功能无需密钥、不花钱),模型**懒加载**(插件先装,模型等第一次真正用到 AI 时后台下载,下载完成前用规则引擎顶替)。
- 智能检测:环境变量/配置文件已有密钥 → 自动识别并测连通性。
- **多模态 UI**:按模型是否支持多模态自动显隐图片功能(provider 自动预设:local_builtin→关 / openrouter→可默认开,勾选框可覆盖)。

### 2.4 引导模块:一次性向导 + 常驻小助手(并行)

- **向导**:首次只介绍最核心三件事;可跳过/重开(设置常驻"重新引导")。**常驻助手**:按需出现,先看用户进度状态再答,避免重复教学。
- **引导内容 = 易腐数据,不是代码**:写成 `.md` + 日期 + YAML frontmatter(`date`/`version`/`applies_to`/`checksum`);过期判定(如 90 天)显示"可能已过期"+ 尝试拉新版;服务端下发 + 热更新。
- **对抗注册界面改版**:
  - ❌ 教用户"点这里、填那里" → 会被页面改版打死
  - ✅ "去注册一个密钥,粘回来,我帮你检查能不能用" → 端到端校验密钥有效性;验证内部结果永远不过期,版本号是第二道防线。
- **分级引导**(同一套内容三详略):萌新→绿袍智者(傻子村民也看得懂)/ 进阶→图书管理员 / 开发者→普通村民。
- **失败路径设计**:密钥填错/额度用完/云端挂了都要有**友好兜底文案**,不弹红框报错(萌新第一次体验崩塌常在失败路径)。
- **进度勋章**:完成注册=「图书管理员」/ 填好密钥=「附魔师」/ 触发性能提示=「别在岩浆旁玩电脑」。
- **数据驱动迭代**:匿名漏斗埋点(哪步卡最久/跳过率最高);离线降级(拉不到最新内容显示缓存版并标注)。
- **答问知识库分层**:FAQ 模糊匹配(规则引擎) → 小模型 → 云端。

### 2.5 Mod 翻译模块(§3,已落地 v0.3.0)

> 该点在 AI规划 §3(方案)与 ROADMAP(v0.3.0 已实现)均有涉及,此处归并为一处。
- 场景:mod 选择 / 详情界面,本地小模型做翻译;来源:`mod_translate.py`。
- **术语表注入**:MC 标准译名(下界/末地/附魔/刷怪笼…)进 system prompt(实际 93 条标准译名术语表)。
- **翻译缓存**绑定模型版本号(模型更新即作废)+ 置信度标记(低置信标注"机翻仅供参考")。
- **避开游戏时段**:批量翻译排队到游戏运行之外执行。
- **人设联动**:绿袍智者说"这段原文有点绕,我翻得可能不准,但大意是……"。
- **结果**:资源中心 Mod 详情显示中文翻译 + "机翻仅供参考" + 设置开关 `ai_mod_translate`。

### 2.6 性能策略(§5)

| 策略 | 说明 |
|---|---|
| 量化提示 | 显示"当前模型推理占用 CPU 约 X%" |
| 主动避让 | 检测到游戏进程启动/聚焦 → 推理暂停或降优先级;游戏进程优先级始终高于推理 |
| 用完即卸 | 任务完成立刻卸载模型回到几百 KB 空闲态(常驻内存是隐蔽性能杀手) |
| 温控意识 | 笔记本过热降频,高温时劝退重任务 |
| 冷启动策略 | 借游戏启动加载空闲时间预加载模型 → AI 功能"秒开"(待验证,已记录) |

### 2.7 游戏启动时模型去留(`ai_in_game` 三档,§5.1)

| ai_in_game | 含义 | 游戏启动卸载策略 | 适用 |
|---|---|---|---|
| `off`(默认) | 游戏内不用 AI | 卸载本地模型(省内存给游戏) | 大多数玩家 |
| `cloud` | 游戏内 AI 走云端 | 卸载本地模型(游戏内走云端) | 游戏内辅助复杂、用更强模型 |
| `local` | 游戏内 AI 走本地模型 | **不卸载**,模型常驻服务游戏内请求;但要降推理优先级 + 内存监控 | 离线玩家,游戏内轻量辅助 |

联动:off/cloud → `engine.stop()`(卸载);local → 保持加载,降推理优先级,内存吃紧则提示或临时卸;游戏退出 → 按空闲策略决定是否重新加载。

### 2.8 本地 AI 技术要点(§7)

1. **grammar 约束解码(最优先)**:llama.cpp GBNF 强制结构化输出,小模型只能吐合法 JSON 且字段齐全(从根上解决"小模型不听话"格式问题)。
2. **安全:mod 简介是不可信内容** —— 防 prompt injection,mod 内容永远只作"待处理数据"传入,与系统指令严格分离,从 mod 内容提取的"指令"一律忽略(架构层面定死)。
3. **本地知识注入**(最轻量 RAG):已装 mod 清单 / 游戏版本 / 最近崩溃日志作上下文喂给模型;实测注入上下文综合 73.9% vs 无上下文 66.9%(+7.0pp)。
4. **回归测试集**:固定用例 + 期望输出,模型/规则更新后自动跑防回退(`python local_ai.py --regress`,31 条 ×3 次)。
5. **模型文件校验**:下载时 sha256 校验 + 版本钉住,防损坏/中间人替换。
6. **数据分级与隐私**:玩家名/账户信息/世界内容绝不离开本地;边界写进隐私说明。

### 2.9 模型选型(§8.1,已拍板)

**最终模型:xLAM 微调版 `qwen3.5-0.8b-function-calling-xlam.q4_k_m.gguf`** ⭐(本地**只养一个模型通吃**,不做多模型分场景调度)。

| 模型 | 工具名准确率 | 参数准确率 | 综合 |
|---|---|---|---|
| 通用版 `Qwen3.5-0.8B-q4_k_m` | 51.6% | 51.6% | 51.6% |
| **xLAM 微调版** | 74.2% | 69.4% | **71.8%** |

- 量化依据:Q4_K_M 是体积与工具调用可靠性的甜点;过低量化(Q2/Q3)破坏函数选择与 JSON 输出;grammar 约束不能省(量化是"概率上更稳",grammar 是"结构上必对")。
- 已否决:分场景专用小模型 ❌(调度复杂,收益在启动器场景不可感知);硬件自适应量化档位 ⏳(等真实使用数据)。
- 模型文件格式 GGUF、mmap 加载策略 ✅ 确定走 llama.cpp 生态。

### 2.10 版本化矩阵(§6)

四个独立维度:引导内容版本(跟随外部世界)/ 启动器版本(发版)/ 云端 API 契约版本(服务端接口)/ 模型版本(模型更新)。
> 考虑统一资源清单文件管理所有可更新资源(模型/规则集/术语表/引导),所有更新走同一套机制。

---

## 3. 灵感清单(#1 ~ #14)

> 攒着"以后想做"的想法;实现时优先挑对用户最有价值、改动最小的。

- **灵感 #1 标记"模组生态黄金版本" 🏅** — ✅ **已完成**(2026-08-24)。用标记指定版本方式落地;数据版(按游戏版本统计 Mod 数)建议时机:阶段 3 Mod 管理做完后顺手做。
- **灵感 #2 联机方案中心 🌐** — 已记录(2026 年 2 月)。按场景推荐(虚拟局域网 EasyTier/Radmin/ZeroTier、内网穿透 SakuraFrp/frp、联机 Mod、官方方案);远景 = 整合进启动器 + Agent 自动配置(自动判断场景、给步骤、装好配好、一键加入).建议时机:先在灵感清单细化各方案适用场景/接入成本,阶段 2/5 后逐项落地。
- **灵感 #3 NeoForge 支持** — ✅ 已实现(2026 年 8 月)。接口 `maven.neoforged.net/.../neoforge-<ver>-installer.jar`(GET 正常,须避免 HEAD 被拒);复用 Forge 同款安装管线;模块路径启动已处理。遗留:整合包/依赖生态细节待实测。
- **灵感 #4 Mod 兼容性提示 ⚠️** — 已记录(2026 年 2 月)。Modrinth API 有 dependencies;"潜在不兼容"需社区数据。建议时机:Mod 下载稳定后先做"依赖自动提示",再探索社区信息源。
- **灵感 #5 Mod 关系网状图 🕸️** — 已记录(2026 年 2 月)。数据源 Modrinth dependencies 字段;画图用 QGraphicsScene 或 Web 可视化。建议时机:灵感 #4 之后,进阶可视化。
- **灵感 #6 备份机制(防坏档)🛡️** — 已记录(2026 年 2 月)。**部分已实现**(CLI `backup <实例>` + AI 装 Mod 前自动备份,2026 年 8 月)。备份目录 `.minecraft/backups/<实例>/<时间戳>/`,zipfile 打存档包;模组列表 txt;与 AI 权限系统联动(写操作前强制备份)。建议时机:补 GUI 备份按钮 + 整合包操作前自动备份(是"AI 自动测试"的安全前置)。
- **灵感 #7 做教程 📖** — 已记录(2026 年 8 月)。内置首次引导页 / 功能说明 / 常见问题(杀软误报、离线账号、备份回滚)。建议时机:功能稳定后、发朋友前必做(减 80% "怎么用"答疑)。
- **灵感 #8 跨平台 🌍** — 已记录(2026 年 8 月)。Python+PySide6 天然跨平台;需处理的 Windows 专属点:`os.startfile()`→`QDesktopServices.openUrl`、路径用 `os.path.join`、Java 探测按平台分支、打包(Win exe / macOS .app / Linux AppImage)。建议时机:功能稳定后集中;优先改 `os.startfile`。
- **灵感 #9 dry-run 机制 🧪** — 已记录(2026 年 8 月)。写操作前先演练(预览改哪些文件/下载什么/多大/装哪,确认再执行)。应用场景:AI 工具写操作清单、装 Mod 显示文件列表、改配置先展示内容。实现思路:写操作统一走 `dry_run` 开关,工具层生成"变更描述",GUI 弹确认框;与灵感 #6 互补(先备份 → 再演练 → 再执行)。建议时机:AI 权限系统稳定后作为"工作区可写"进阶保险。
- **灵感 #10 自制"指令桥"mod(游戏指令终极方案)🧩** — 已记录(2026 年 8 月,论证后确认)。**方案论证**:Java Agent(字节码改写)→ 脆弱、无生态、做不了高级功能 → **放弃**;多平台 mod(Fabric+Forge+NeoForge)→ 成熟生态、能拿注册表数据/挂事件钩子 → **采用**。**勘误(2026-08-23)**:bridge-mod 内部已整合指令口(本地 TCP 指令口 + CommandSource 精确反馈),**不负责开启 RCON**;RCON 是另一条独立长期备用通道。**mod 功能路线图**:① 指令桥(进世界自动开本地指令口 + 命令结果写回文件)② JEI/配方数据导出(RecipeManager dump 成 JSON → 套娃合成/物品参数比较)③ 指令库同步 ④ 未来更多。**结构**:工作区 `bridge-mod/`(MultiLoader 骨架);数据导出到实例目录 `.bridge/`。**构建**:✅ fabric 编译通过;JAVA_HOME=java-runtime-delta(Java 21),Gradle 必须 8.10.x(8.13/8.14 移除 `Problems.forNamespace` 导致 Loom 1.6 崩溃);common 并入 fabric;forge/neoforge 插件待适配。**兼容目标**:Fabric 1.21.1 / NeoForge 1.21.1 已发布;目标 1.20.1 / 1.21.4;Forge(1.19 及以前)晚点。**老版本兼容**:单项目同时支持 1.7.10→1.21 不现实;现实路径 = 现代版本用本 mod,老版本沿用 "Lan Server Properties + 模拟按键 + 日志反馈"。
- **灵感 #11 物品数据"注册表直取" + 老版本兼容需求 📦** — 已记录(2026 年 8 月,用户洞察)。关键认知:物品管理/配方数据源**本质在游戏注册表 + RecipeManager**,mod 只是"导出管道";真正离不开 mod 的只有**指令桥**和**命令结果反馈**。推论:未来不要 mod 也能拿原版物品数据(解析 data/resource JSON,代价是看不到 mod 物品);指令桥仍绕不开 mod。老版本兼容需求(待触发):老 Forge、API 完全不同(无 BuiltInRegistries/DataComponents/Holder),导出代码按版本重写;轻量替代 = 原版物品走 datapack 解析 + 指令桥用现成 RCON mod;只有"批量自动化指令/精确反馈"成硬需求时才建老工具链分支。现状:不实现,需求触发时按上评估。
- **灵感 #12 bridge-mod 分发策略(已定)📦** — 决策(2026 年 8 月):❌ 不做"启动器内置源码自动编译"(朋友电脑没 JDK/Gradle/缓存);采用**预编译 jar + 双通道分发**(Modrinth 发布为主规范 + GitHub Releases 备/镜像)。分发通道:① 离线(三平台 jar 打进 exe,"一键配置"从内置资源复制到实例 mods,零联网无感)② 在线(「检查桥 mod 更新」从 Modrinth/GitHub 拉新 jar)。版本管理:内置"桥 mod 版本表"{loader:{mc版本:{版本,URL}}};迭代 = 编译 → 发布 → 更新版本表。现状:策略已定,待 forge/neoforge 可编译后落地。
- **灵感 #13 宣传视频 📹** — 已记录(2026-08-24)。前置:本版 UI + emoji 优化收尾。内容方向:核心卖点 = AI 助手(对话/工具调用/本地模型离线可用)、一键装 Mod、查配方(无需进游戏)、联机方案中心;演示脚本:安装 → 选版本 → 问 AI → 装 Mod → 进游戏。建议时机:本版优化完成后启动。
- **灵感 #14 规则引擎 AI 化 + 本地模型定位 🤖** — 已记录(2026-08-24)。方向:① 规则引擎可能 AI 化(给本地模型更多自由度,减少硬规则兜底)② 提高引导用户配置云端模型的优先级(云端当"大脑",本地小模型当"引导")③ 本地小模型定位:现阶段似乎只能做引导。技术观望:内存/带宽/NPU 普及;远景白日梦本地跑大模型。现状:**不急于改架构**,先记方向;**提醒:避免过度投入规则引擎优化**(未来可能被模型化取代);近期可做 = 引导用户配置云端的文案/流程。
- **灵感 #15 触控兼容 + 安卓端 📱(不急,先记录)** — 已记录(2026-08-24,用户)。**① 触控兼容**可与**灵感 #8 跨平台**一起先做/同批考虑(触摸/触屏、小屏、平板类界面适配)。**② 理想情况做 Android 端**(安卓玩家/小朋友是 MC 未来主力);但**用户强调:等 token/资源充足后再试错**,当前**只记录方向、不排期**。建议时机:跨平台(灵感#8)推进时一并评估触控;安卓端另行立项(待资源充裕)。

---

## 4. AI 关键补充

### 4.1 模型下载与镜像

- 模型下载源:hf-mirror 优先 + HF 兜底(2026-08-23 直连可用)。
- 测试阶段用本地模型镜像服务器(`dev_model_server.py`,默认 8765,服务 `AMCL/models/`)秒下大文件;sha256 校验通过后 `download()` 直接跳过。

### 4.2 测试阶段密钥管理(临时措施)

- 现状:一个共享密钥给朋友测试(已知悉不安全)。风险:账单、权限不可控、泄露只是时间问题。
- 缓解:OpenRouter 子密钥(每人一个,额度上限)/ 极薄代理(限额转发)/ 平台消费告警必须开。
- 正式版:用户自带密钥 + 开箱即用的本地基础功能。

### 4.3 引导模块关键设计

- **关键前提**:助手必须知道用户进度状态(已填密钥?已装插件?),答问前先看状态,避免重复教学。
- **引导内容 = 易腐数据**:`.md` + 日期 + YAML frontmatter(.md 可进 git diff);过期判定显示"可能已过期";服务端下发 + 热更新,跟随外部世界不跟启动器发版。

---

## 5. 指令通道架构(游戏指令)

**通道优先级(见 `game_command.py`)**:`bridge 指令桥` → `RCON` → `模拟按键`。

1. **bridge-mod(优先,推荐)**:自制 mod 内部**整合指令口**(本地 TCP `127.0.0.1:26100`,不依赖 RCON 协议、无需对局域网开放)+ 自定义 `CommandSource` 把命令结果**写回文件** → 启动器 100% 准确反馈。数据导出到实例目录 `.bridge/`(如 `.bridge/recipes.json`、`.bridge/items.json`)。
2. **RCON(长期备用通道)**:RCON 客户端 + `server.properties` 自动配置 + 日志增量反馈;配合 **Lan Server Properties**。服务 bridge-mod 未覆盖的**老版本/非黄金版本**。
3. **模拟按键(兜底)**:`os.startfile` 之外的最后手段,配合日志反馈。

> ⚠️ **勘误(2026-08-23)**:bridge-mod **不负责开启 RCON**——RCON 是独立通道,与 bridge 无耦合;bridge 内部已整合 RCON 想实现的能力(本地指令口 + 精确反馈)。GUI「发送游戏指令」菜单入口已隐藏(由指令中心 skill 与 bridge-mod 通道替代)。

---

## 6. 文件放置约定(AMCL 私有数据目录)⭐

> **核心原则**:`AMCL/` = **启动器私有数据目录** —— 启动器需要下载/生成的一切文件都放进 `AMCL/`(或启动器自己的目录树内),**绝不往用户系统目录散落**(`%APPDATA%`/`%TEMP%`/文档/桌面等)。便携原则见 `paths.py`。

**AI 部分(已落实)**:

| 路径 | 内容 | 来源模块 |
|---|---|---|
| `AMCL/config.json` | 启动器设置(含 AI 设置、镜像策略、多模态开关) | settings.py |
| `AMCL/models/` | 本地模型 GGUF + `manifest.json`(sha256 钉住) | model_registry.py |
| `AMCL/runtime/llama-cpp/` | llama.cpp 服务器与依赖(本地推理运行时) | local_ai.py |
| `AMCL/cache/recipes-jar/` | 配方旁路解析缓存(带签名自动失效) | recipe_datapack.py |

**同一原则的其它**:一切新下载/生成文件(运行时、缓存、临时文件、清单、日志)一律放启动器目录内:设置/私有数据 → `AMCL/`;游戏数据 → `.minecraft/`;其余 → 启动器目录下专属子目录。新增功能落地前先问一句"**文件放哪**":答不上来 = 先定目录再写代码,禁止默认写用户目录。**唯一例外**:启动器所在目录不可写(如 Program Files)时,`paths.py` 自动整体退回 `%APPDATA%\AgentMinecraftLauncher`(仍是**一个**专用文件夹,不是散落)。

> **补充(2026-08-24)**:`AMCL/` 在 `.gitignore` 中,是**运行时私有数据/本地生成物**,不进 Git。**随发行版分发、需进 GitHub 的产品数据**(如 Mod 中文名清单 `mod_cn_ext.json`)放**仓库根**(与代码同层,被 Git 跟踪),**不放 `AMCL/`**。二者区别:产品数据 = 仓库自建、随版本发布;运行时数据 = 启动器本地生成、勿入库。

---

## 7. 相关拆分文件(仍保留,供追溯)

- **`项目规划.md`**(草稿 v1):总产品定位 / 功能拆解 / 分阶段路线图 / 关键技术调研(含 PCL 中文搜索、MC百科 API 现状)。
- **`AI规划.md`**(草稿 v1):AI 模块细节(路由/引导/翻译/性能/本地技术要点)+ **决策记录表 §10** + 文件放置约定 §11。
- **`ROADMAP.md`**:路线图勾选(已完成/规划中/候选)。
- **`测试指南.md`**:测试流程与用例(非规划,一并放在仓库根)。

---

## 附录:跨文档冲突 / 归并结论

> 合并时发现以下点在多份文档重复或表述不一,已做归并,结论如下(如需历史细节查对应拆分文档):

| 冲突点 | 出现位置 | 归并结论 |
|---|---|---|
| 「本地模型翻译」 | AI规划 §3(方案) + ROADMAP / CHANGELOG v0.3.0(已落地) | 归并到 §2.5;**结论:已落地 v0.3.0**(93 条译名术语表 + 本地翻译引擎 + 缓存绑定模型指纹 + 设置开关 `ai_mod_translate`);保留 AI规划 §3 设计要点 |
| 「指令通道」 | 项目规划 灵感 #10 + ROADMAP(职责勘误) | 归并到 §5;**结论:bridge 内部整合指令口,不负责开 RCON;通道 = bridge → RCON → 模拟按键** |
| 「模型选型」 | AI规划 §8.1 + ROADMAP(选型拍板) | 一致;以 **AI规划 §8.1** 为准(最终 xLAM `qwen3.5-0.8b-function-calling-xlam.q4_k_m.gguf`,71.8% vs 51.6%) |
| 「任务路由/失败链路」 | AI规划 §1 + ROADMAP(§1 落地) | 一致;归并到 §2.2,以 AI规划 §1 规则为准 |
| 「grammar 约束解码」 | AI规划 §7.1/§8.2 + ROADMAP(原型) | 一致;归并到 §2.8,以 AI规划 §8.2 实测为准(参数 +9.6%) |
| 「文件放置 AMCL/」 | AI规划 §11 + 项目规划(AMCL/models) | 归并到 §6;**补充:产品数据(如 mod_cn_ext.json)放仓库根,不进 AMCL(AMCL 被 gitignore,是运行时私有数据)** |
| 「下一版目标(2026-08-24)」 | ROADMAP(规划中,未勾选) | 本轮已落地,已在 §1 标记 ✅(资源中心默认加载 / Mod 中文名对照表) |
