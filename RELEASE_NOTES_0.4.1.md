# v0.4.1 更新说明(面向玩家 / 朋友)

> 一句话:这版是**正式版前的大更新预览**——加了**微软正版登录**,整合包导出能生成 **Modrinth .mrpack**(可直接导入/分享),实例详情也更好用了。

## 这次更新能干嘛

**1. 微软正版登录(重要)**
- 登录卡片 → 「更改登录方式 ▾」→「微软正版登录」:启动器给你一个网址 + 代码,去浏览器授权就行。
- 登录后进游戏用的是**你正版账号的名字 + 真实 UUID**,连支持正版验证的服务器(online-mode)都没问题。
- 离线模式也在:登录方式切回「离线」即可,用昵称 UUID 稳定(不再每次变)。
- 会自动记住登录;**不用每次启动都重登**(自动刷新令牌)。
- 头像会显示你账号的真实皮肤头像(拉不到就用占位色)。

**2. 整合包导出 → Modrinth .mrpack**
- 实例详情新增「导出」:像 Windows 选安装路径那样,按文件勾选(默认勾 mods/config/光影/资源包,不勾存档/日志等个人记录)。
- 能选格式:**Modrinth .mrpack**(推荐,可直接导入本启动器/上传 Modrinth分享)或 扁平 .zip。
- .mrpack 会自动把**能匹配到 Modrinth 的 mod 走 Modrinth 下载 + 自动带依赖**;本地/小众 mod 放进 overrides。
- 可选「是否带上启动器」做成两层压缩包(朋友解压即装)。

**3. 实例详情更好用**
- 新增「概览」(信息 + 一键启动/打开目录)、「设置」(单实例内存)、「导出」、「投影原理图」(装机械动力时出现)。

## 怎么用
- 正版登录:我的版本 → 左上登录卡片 → 更改登录方式 → 微软正版登录。
- 导出整合包:实例详情 → 导出 → 勾文件 → 选格式(.mrpack 或 .zip)→ 导出。

> 测试说明:正版登录/Modrinth 导出都需联网;正版登录时要在浏览器用你自己的微软 Minecraft 账号授权。
> 这是正式版前的**预览/测试版**,欢迎反馈问题。
