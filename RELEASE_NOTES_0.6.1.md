# v0.6.1 更新说明(面向玩家 / 朋友)

> 一句话:这版重点修了**旧版本(Forge 1.16.5)的兼容**和**游戏内 AI 发指令的权限误判**,顺带修了一批下载/实例相关的小毛病。属于 0.6.0 的修复版。

## 这版主要修了什么

**1. 旧版本 Forge 1.16.5 能玩、能用了**
- 0.6.0 对老版 Forge(1.16.x)支持不好,装的依赖/启动类路径老是出错;
- 现在:老版 Forge 自动用启动器管理的 **Java 8**,修正运行库(universal)映射与 Maven 下载源,修复安装器产物缺失,Forge 1.16.5 也能配 bridge-mod / 走游戏内 AI 指令通道。

**2. 游戏内 AI /ai 发指令不再误报"没权限"**
- 之前单人游戏里用 `/ai` 让 AI 发指令(如改时间),会莫名其妙说"你没权限";
- 修好了启动器端权限判定:单人/局域网房主默认允许(能否成功由世界的"允许作弊"决定),只对"连的服务器里非 OP 玩家"才收紧。

**3. 一些体验修复**
- 启动器默认选中并**可以直接启动上次游玩的实例**;
- 修复"共享 mods 目录误加载 bridge-mod"、并行下载圆环进度反复回转、Maven 下载失败的诊断提示更清楚等。

## 说明
- 这是**给测试用**的一版(还没正式公开发布)。确认没问题后会正式放出来。
- 版本号 0.6.0 存在若干问题,**请勿使用 0.6.0**,直接用本版。

## 怎么用
- 替换 `AgentMinecraftLauncher.exe` 即可;对已有实例、设置、存档无影响。
