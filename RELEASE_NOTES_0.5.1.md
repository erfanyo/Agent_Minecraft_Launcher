# v0.5.1 更新说明(面向玩家 / 朋友)

> 一句话:这版是一堆**实用修复**——AI 助手更会"装 Mod / 整合包"了、选实例不会再莫名被取消、引导教程的箭头对得准了,顺便教你怎么一次性解决"智能应用控制"的拦截。

## 这版修了什么

**1. AI 助手:能正确"找到"装 Mod / 整合包的工具了**
- 之前你说"我想玩 <某个 mod / 整合包>",AI 却调不动"下载/安装"类工具,任务会拆岔。
- 现在 AI 能识别"**想玩 / 来玩 / 帮我装**"这类说法,以及"机械动力 / 航空 / create"等词,
  从而正确调用搜索、下载、装 Mod、装整合包的工具。装之前它会先搜、再装,拆解更合理。

**2. AI 提问框:默认高亮改成「确定」**
- 之前 AI 要你选东西时,弹窗默认高亮/回车落在「**取消**」,一不留神回车就拒绝了(不合逻辑)。
- 现在默认高亮在「**确定**」,回车即确认;取消绝不会因回车误触。

**3. 选实例不再"被自动取消"**
- 之前选完一个实例,详情页偶尔会自动收起来(其实是后台刷新把选中项清掉了)。
- 现在刷新实例列表时会**记住你选中的实例**,不再自动取消,详情页稳稳地显示。

**4. 引导教程的箭头/遮罩对得更准了**
- 之前几处高亮框对不齐(因为界面改过、原来的目标控件挪位/藏起来了)。
- 现在对准搜索框、插件页等当前真实控件,高亮框精确罩在目标上。

**5. 打包 & 更新链路**
- 版本号升到 v0.5.1;内置"检查更新"能正确识别升级。

## 如果你被"应用程序控制策略阻止此文件"拦了
Windows 11 的**智能应用控制**会拦未签名的新软件,而且**没有**"仍要运行"按钮。
一次性放行/关闭(按钮就叫这些名字):

> **Windows 安全中心 → 应用和浏览器控制 → 智能应用控制设置 → 关**

⚠️ 小提醒:部分系统版本**关了就回不到"开"**(除非清理系统/重装);这只是本机放行的办法。

## 怎么用
- 直接替换 `AgentMinecraftLauncher.exe` 即可,对已有实例、设置、存档**无影响**。

## 发布校验
- 作者公钥 `erfanyo.asc`,GPG 校验方法见 `README.md`。
- SHA256 见下方(或 Release 说明)。
