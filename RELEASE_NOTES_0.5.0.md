# v0.5.0 更新说明(面向玩家 / 朋友)

> 一句话:这版做了**界面全面现代化 + 新增收藏夹**——启动器更好看、更通透,下载 Mod 时能一键收藏、管理常玩的 Mod 了。

## 这版的新东西

**1. 界面全面改头换面(重要)**
- 统一的颜色与样式体系:深色 / 浅色两套主题都重做了一遍,配色、圆角、卡片层次更顺眼。
- **透明磨砂壁纸**:设置里可以开背景壁纸(内置渐变 / 自定义图片),界面上的卡片、列表、下拉框都变成半透明的,背景透出来,整体更通透、不闷。
- 细节动画:切页、面板展开收起有柔和过渡。

**2. 下载新资源更好用了**
- 下载页(Mod / 光影 / 数据包 / 资源包)重新排布:左边搜索列表、右边详情,底部一条「手动下载 / 目标实例 / 下载」操作栏,想装哪个版本、装到哪个实例,点开就选,一目了然。
- 列表支持**无限滚动**——往下拉自动加载更多,不用翻页。
- Mod 详情、版本、加载器、目标实例这些面板都改成透明圆角,不再是一块块实心灰板。(注:加载器下拉框目前仍是较实的底色,后续再打磨。)

**3. 新增收藏夹(新功能)**
- 下载任意 Mod 时,详情里的「☆ 收藏该版本」一键收藏(连同当前选的版本一起),常玩的 Mod 不用再记名字。
- 「收藏」页里按文件夹管理:可以新建多个收藏夹,**把收藏卡片拖到别的文件夹 = 复制过去**,方便按「给儿子装的 / 自己用的 / 整合包用」分类。
- 收藏数据存在启动器目录的 `favorites.json`,可手动备份。

## 怎么用
- 直接下载替换 `AgentMinecraftLauncher.exe` 即可;对已有的实例、设置、存档**无任何影响**。
- 收藏夹入口在左侧主菜单;背景壁纸在 **设置 → 界面** 里开启。
- 想看动画/透明效果的,建议先在设置里开一张背景壁纸(渐变即可)。

## 发布校验
- 本版与签名对应的作者公钥见 `erfanyo.asc`;校验方法见 `README.md`「校验完整性」。
- SHA256 见下方(或 Release 说明)。
