# v0.4.0 更新说明(面向玩家 / 朋友)

> 一句话:这版**终于不用再管"实例列表要手动刷新"**、深色模式看得清了、图标更统一了,
> 而且 Forge 1.20.1 的桥 mod 也正式支持了。

## 这次更新能干嘛

**1. 实例列表自动刷新**
之前换了游戏目录/外头丢了个实例进去,「实例(共x个)」要手动点刷新或切走再切回才变。
现在只要 `versions/` 里**新增或删除了实例文件夹,列表和数量会自动更新**,不用手动刷。

**2. 深色模式修好了一堆看不清的字**
- 实例详情里 Mod 列表的文字,之前深色模式下是**黑底黑字**看不见;现在改用主题色(启用亮/禁用灰)。
- 「更新日志」正文、以及数据包/光影包列表,也都在深色模式下用上正确颜色了。

**3. 图标更统一了(不再是零零散散的 emoji)**
- 换上一套**极简单色线框图标**,颜色跟着你的主题(深色/浅色/自定义配色/色盲模板)自动变。
- 第一批:下载新资源左侧的 8 个菜单(首页/实例/整合包/Mod/光影/数据/资源/插件)。

**4. Forge 1.20.1 也能用桥 mod + 游戏内 AI 了**
- 以前桥 mod 只支持 Fabric 1.21.1 和 NeoForge 1.21.1,现在补上了 **Forge 1.20.1**(很多人还在玩的版本)。
- 装好后游戏里敲 `/ai 你的问题` 就能直接问启动器 AI,结果回显在游戏聊天里。

**5. 桥 mod 以后出新版不用再改启动器(自动发现)**
- 启动器现在**按文件名自动识别**对应的桥 mod jar(从打包内置 / 本地 dist / GitHub Releases 找),
  新版本/新加载器/新 MC 版本只要发布 jar 就能用,不用每次改启动器。

**6. 一键配置更聪明**
- 版本**有**对应桥 mod → 自动下载;已装但**旧了** → 提示就能一键更新;
  版本**不支持** → 明确告诉你"还不支持",并引导改走临时 RCON。

**7. 崩溃诊断能"动手修"了(进阶,默认关)**
- 在「技能管理」里开启「崩溃诊断·自主修复回路」后,AI 分析完崩溃,**能帮你自动修**的部分
  (如内存不够→自动加内存、冲突 Mod→自动重装一个兼容版)会在**你同意 + 有写权限**时直接动手,
  并验证有没有修好;改不了的(显卡驱动/硬件/要删存档)会明说,不乱来。

## 怎么用
- 升级后直接打开:实例列表自动刷新、深色模式看得清、图标更统一。
- Forge 1.20.1 想用游戏内 AI:「我的实例」选中它 →「一键配置 ▾」→「一键配置 bridge-mod」→ 进游戏敲 `/ai`。
- 想让 AI 帮修崩溃:「设置 → AI 助手」里把权限调到「工作区可写」,再到「技能管理」开启「崩溃诊断·自主修复回路」。
