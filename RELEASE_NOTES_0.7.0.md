# v0.7.0 更新说明(面向玩家 / 朋友)

> 一句话:这版最大的亮点是 **📌 AI 上下文置顶(图钉)**——你能把关心的东西"钉"住让 AI 记住,
> 回答更懂你;同时底子也重构得更稳(后台任务/实例重命名/已装资源卡片),并支持了 **CurseForge** 和 **bridge-mod Forge 1.12.2**。

## ⭐ 本期亮点:AI 上下文置顶(图钉)
- 聊天时可以把关键上下文**钉住**,AI 会一直记得、回答更贴合你当前在做什么;
- **只注入上下文,不给 AI 额外操作权限**(安全)。
- 这是这版最"看得见"的新交互,建议开箱就试试。

## 这版的新东西

**1. 后台任务统一(更稳、不卡)**
- 下载、启动游戏等耗时操作统一走"后台任务":有清晰**进度、可取消、可重试**,界面不卡,右下角圆环 + 详情页更一致。
- 遇到网络/下载失败,提示更明确(拒绝访问、磁盘满、文件不存在等各有建议)。

**2. 实例重命名**
- 给已建好的实例改个更顺手的名字,会自动同步它的 **ID、安装目录、继承引用**,不会留下断链。

**3. 已安装资源卡片**
- 某个实例已装的 Mod/资源,以**卡片形式**一眼浏览(本地信息,不用联网)。

**4. CurseForge 支持**
- Mod 搜索/下载除了 Modrinth,多了一个 CurseForge 来源,找海外 mod 更容易。

**5. bridge-mod:支持 Forge 1.12.2**
- 老版 Forge 1.12.2 现在也有 bridge-mod(核心兼容实现,已编译,待实机联调)。

**6. 崩溃诊断 + 日志脱敏**
- 崩溃时给出更清晰的诊断原因;
- 启动器日志、AI 操作记录**自动脱敏**(密钥/登录令牌/常见凭证不落盘明文)。

**7. 其它**
- 游戏内 `/ai` 权限按**游戏类型**判定(单机/局域网房主可用;连的服务器只看 OP)。
- 依赖导入、Mod 依赖网络、启动命令等更完善。

## 怎么用
- 直接替换 `AgentMinecraftLauncher.exe` 即可;对已有实例、设置、存档无影响。

## 说明
- 本版功能量较大,首次使用建议先创建/重命名一个实例、跑一次下载与启动验证。
