# v0.6.0 更新说明（玩家版）

这版主要把“下载、日志和旧版本兼容”理顺了。

## 日志在哪？

启动器的重要操作、下载进度和报错都在：`<游戏目录>/logs/launcher.log`。

遇到下载/启动失败时，先把这个文件最后一段发到 GitHub Issue；游戏本身的日志在同目录的 `latest.log` 与 `debug.log`。不要上传账号令牌或整个 `.minecraft`。

云端 AI 的工具调用记录在：`<启动器配置目录>/ai_training/cloud_tool_calls.jsonl`。默认配置目录通常是启动器旁的 `AMCL` 文件夹，即 `AMCL/ai_training/cloud_tool_calls.jsonl`。它适合挑选案例用于 Issue；提交前同样请删除 token、API Key、服务器地址、局域网 IP 和本地绝对路径。

## 这次做了什么

- 下载带 Forge/Fabric 等加载器的版本时，基础原版不再作为一个额外实例显示；它会存入内部版本仓库。
- 修复 Forge 1.18.2 安装器需要临时读取标准原版 jar 路径的问题：补丁期间临时提供，结束后自动收纳。
- 修复关闭“版本隔离”时删除单个实例可能误删整个游戏目录的严重问题；现在只会删除该实例的 `versions/<实例名>`。
- Minecraft 语言可跟随启动器/系统语言（设置中可关闭）。
- Bridge Mod 新增 Forge 1.18.2 完整桥接产物，并完善 Forge 1.19.2 元数据兼容。

## 帮 AI 变得更好

欢迎在 GitHub Issues 提交：AI 对话回答、工具调用记录、失败场景和你的期望结果。请先删掉账号、token、服务器地址等敏感信息。这些真实案例会用于后续评估与微调。
